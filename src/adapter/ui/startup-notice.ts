/**
 * スタートアップお知らせ(#954、user 指示 2026-07-22)。
 *
 * 「起動後のスタートアップメッセージを追加 / オフスイッチありで、
 *  お知らせを表示する / 今回の変更はお知らせに掲載する / 今度から、
 *  そういう運用にしよう」
 *
 * 設計:
 *   - **運用**: user-facing な変更を含む PR は `STARTUP_NOTICES` の先頭
 *     entry に 1 行追記する(新リリース = 新 entry を先頭に追加)。
 *     CLAUDE.md の PR 運用規律に登録済み
 *   - 表示は boot 完了(ready + container)後に 1 回だけ、右下の
 *     **非モーダル**カード(toast と同カテゴリ ── 画面をブロックしない)
 *   - 既読管理: localStorage(`pkc2.startup-notice.seen` = 最後に閉じた
 *     notice id)。同じお知らせは同一ブラウザで 1 回しか出ない
 *   - **オフスイッチ**(2 系統):
 *     a. flag `shell.startup_notice_enabled`(既定 ON、Flags Inspector と
 *        ⚙ Settings の「News」トグルから変更、container に永続化)
 *     b. カード内「今後表示しない」── SET_FLAG で a を OFF に(readonly
 *        container では localStorage fallback)
 *   - embed 中(iframe)は表示しない
 */

import type { Dispatcher } from '../state/dispatcher';
import { defineFlag } from '../../core/flags';
import { getUiPref, setUiPref, removeUiPref } from '../platform/ui-prefs';

export interface StartupNotice {
  /** 一意 id(既読管理キー)。`YYYY-MM-DD-slug` 形式。 */
  readonly id: string;
  readonly date: string;
  readonly title: string;
  readonly items: readonly string[];
}

/**
 * お知らせデータ(新しいものを**先頭**に追加する)。表示されるのは先頭
 * entry のみ ── リリースごとに 1 entry、項目は user 向け文言で簡潔に。
 */
export const STARTUP_NOTICES: readonly StartupNotice[] = [
  {
    id: '2026-07-28-memory-release',
    date: '2026-07-28',
    title: 'メモリの解放:使っていない本文を手放し、左ペインは見えている行だけ描くようにしました',
    items: [
      '🧠 開いていない本文をメモリから手放すようにしました。これまでは一度読んだ本文をすべて抱えたままで、編集を続けるほど増えていく一方でした。今後は合計 200 万文字を超えたぶんから、古い順に手放します(いま選択中・編集中のエントリは対象外。手放した本文は、そのエントリを開いた時に保存先から読み直します)。実測・本文の多いデータ(3,000 エントリ)で、PKC2 自身のメモリ取り分が 451MB → 165MB(−63%)',
      '📋 左ペインを、見えている行だけ描くようにしました(200 行以上のときだけ発動。それ未満は従来どおりです)。実測・3,000 エントリで画面上の要素数 27,651 → 1,013、描画側のメモリ 81.8 → 64.4MB。スクロール位置・↑↓ キー・検索・選択の見え方は変わりません(10 手ぶんの操作を 1 手ずつ突き合わせて確認済み)。従来どおり全行を描かせたい場合は、URL に `?pkc-flag=sidebar.virtual_list=false` を付けてください',
      '⬆ あわせて不具合修正: 選択が画面の外へ移ったのに左ペインがスクロールしないことがある問題を直しました(深くスクロールした状態で ↑↓ キーを押すと、選んだエントリが画面外のままになる)。今後は選んだ行が必ず見えるところまで寄ります',
      '🖱 不具合修正: 左ペインの幅・分割位置・表の列幅などをドラッグしたままウィンドウの外でマウスを離すと、ボタンを押していないのにサイズ変更が続いてしまうことがある問題を修正しました',
    ],
  },
  {
    id: '2026-07-28-storage-engine-indicator',
    date: '2026-07-28',
    title: '保存先エンジンの表示を追加しました',
    items: [
      '📊 Storage Profile(シェルメニュー → データ管理)の先頭に、いま何で保存しているかを表示するようにしました。「IndexedDB(従来の保存先)」/「wasm-sqlite(OPFS・永続)」/「デスクトップ host の native sqlite」を、sqlite のライブラリ版まで含めて表示します',
      'これまで保存先の切り替えは URL のフラグでしか行えず、実際に切り替わったかを確かめる手段が開発者ツールしかありませんでした。動いているか確認できないのは機能が入っていないのと同じ、というご指摘を受けての追加です',
      '⚠ 永続化に失敗して揮発モード(閉じるとデータが消える状態)になっている場合は、赤字で警告を出します',
    ],
  },
  {
    id: '2026-07-27-boot-memory-fixes',
    date: '2026-07-27',
    title: 'メモリ使用量の修正のお知らせ',
    items: [
      '🔴 重要な不具合修正: 添付ファイルの多いデータ(数百 MB 規模)で、起動直後にメモリが数 GB に達して落ちることがある問題を修正しました。初回起動時の内部処理が、必要なのはデータの ID 1 個だけなのに全添付をまとめてメモリに読み込んでいたのが原因です(実測・添付 512KB × 100 件のデータで、起動時のメモリピークが約 300MB → 約 90MB)',
      '改善: 添付の一覧情報(サイズなど)を組み立てる起動後の処理が、途中で中断しても続きから再開するようになりました。これまでは処理の完了時に 1 回だけ保存していたため、大きなデータで処理が完走できないと、起動のたびに全添付を頭から読み直していました。今後は少しずつ保存しながら進むので、数回の起動で必ず完了し、以後は読み直しません(あわせて処理に息継ぎを入れ、起動直後の負荷が集中しないようにしました)',
      'ご注意: 添付の多いデータでは、この一覧情報の構築が走る間(初回起動から数分間)、メモリ使用量が一時的に大きく増えます(実測・500MB のデータで一時的に 1.5GB 前後)。これはブラウザのメモリ回収の性質によるもので、構築が終われば下がり、2 回目以降の起動では起きません。構築が終わる前にブラウザを閉じても、進捗は保存されており次回は続きから再開します',
    ],
  },
  {
    id: '2026-07-26-differential-save-retirement',
    date: '2026-07-26',
    title: '差分保存の提供終了と、自動移行のお知らせ',
    items: [
      '🔴 重要: 差分保存(differential_save)の提供を終了しました。本日から新たにオンにできません。すでにオンにしている場合、次にアプリを開いた時点で自動的に従来形式へ移行します(エントリ・本文・関連・編集履歴・添付はすべてそのまま引き継がれます。お手元で何かする必要はありません)',
      '終了の理由は速度ではなく データの安全性です。差分保存で保存されたデータを古い pkc2.html で開くと、エントリ同士の「関連」が 0 件に見え、その状態で保存すると関連が実際に消えます(編集履歴の並び順も失われます)。PKC2 は単一 HTML なので、古いファイルを手元に残して開く使い方が実際にありえます。この危険を残したままにはできないと判断しました',
      'ご注意(移行の代償): 差分保存は「編集のたびの書き込み量」を大きく減らす設定でした。移行後は書き込み量が増えます(実測・5000 エントリ / 履歴 15000 件で 1 編集あたり 11KB → 25.7MB)。書き込み自体は画面の描画とは別のところで行われるため操作の体感は変わりませんが、保存領域とディスクの消耗は増えます。もともと既定オフ・マニュアルでも非推奨の設定でしたが、お使いだった場合はこの点をご承知おきください',
      '移行は 1 回だけです。移行後の起動は、これまでどおり何も書き込みません',
    ],
  },
  {
    id: '2026-07-25-visual-audit-fixes',
    date: '2026-07-25',
    title: '表示品質の修正のお知らせ',
    items: [
      '不具合修正: About ページの「折りたたみブロック」が折りたためず、記法(:::details …)がそのまま文字として見えていた問題を修正しました',
      '不具合修正: タイトルが空のエントリで、ファイラーやパンくずに内部 ID(lid)がそのまま表示されていた問題を修正しました(「(untitled)」表示に統一)',
      '整理: 右クリックメニューの「📊 グラフビュー」と Alt+5 を削除しました(グラフビューは廃止済みで、押しても何も起きない項目が残っていました)。Alt+6 のランチャーは変わりません',
      '改善: 階層が深くてツリーに表示しきれないフォルダに「…N」を表示するようになりました。これまでは深い階層の中身がツリーから黙って消え、しかも子の件数が「(0)」と表示されていました(実件数を表示します。中身はファイラー・検索から辿れます)',
      '改善: 長いタイトルのパンくずが「…」で省略されるようになりました(全文はマウスを乗せると出ます)。これまでは横スクロールに逃げていて、省略されていること自体が分かりませんでした',
      '不具合修正: 中身が見つからない添付ファイルが「⏳ ファイル読み込み中…」のまま永久に止まる問題を修正しました(保存領域に実体が無いと確定したら「⚠ ファイルの中身が見つかりません」と表示します。ダウンロードボタンはそのまま試せます)',
      '安全策: 内容を読み取れないスプレッドシートを開いたとき、警告を表示して元データを保持するようになりました。これまでは空のシートと見分けがつかず、そのまま保存すると元データが空で上書きされていました(作り直したいときは「空のシートで作り直す」を押してください)',
      '改善: 画面の文言を日本語に揃えました。サイドバー(検索欄・最近・絞り込み・空状態・操作ヒント)、メタペイン(タグ / 分類 / フォルダ / プロパティ / 参照と関連 / 関連 / 被参照 / 本文リンク / 欠損リンク)、添付カード(名前を変更 / ダウンロード / プレビュー種別)が対象です。「Copy link」など用語として定着している語はそのままです',
      'ご注意: メタペインの見出しが変わりました ── Outgoing relations → 関連 / Backlinks → 被参照 / Outgoing links → 本文リンク / Broken links → 欠損リンク。マニュアル(05・09 章)も更新済みです',
      '改善: スプレッドシートのセルが 1 行表示になり、はみ出す分は「…」で省略されるようになりました(Excel と同じ既定)。これまでは長い文章のセルが 1 文字ずつ折り返して行の高さが数十倍に伸びていました。全文はマウスを乗せると出ます。編集中はクリックしたセルだけ全文が開きます(列幅はこれまでどおりドラッグで変えられます)',
      '改善: テキスト系の添付ファイル(.txt / .json / .yaml / .csv / .log / .conf など)がその場でプレビューできるようになりました。これまでは「✎ 編集」はできるのに「プレビューできません」と表示される、ちぐはぐな状態でした(大きなファイルは先頭だけ表示し、続きは編集かダウンロードで見られます)',
    ],
  },
  {
    id: '2026-07-25-code-edit-lite-wave',
    date: '2026-07-25',
    title: '2026-07-25 更新のお知らせ',
    items: [
      '新機能: Flags Inspector に「{} JSON」一括編集を追加しました。VSCode の settings.json のように、変更済みの flag をハイライト・補完つきの JSON でまとめて編集できます(誤りは行番号つきで表示、適用は差分だけ・再読み込み不要)',
      '不具合修正: コード編集(flags JSON / コードブロック / 添付)で、入力にエラーがあるのにエラー内容が画面に表示されないことがある問題を修正しました(行番号つきで表示され、修正するまで適用できません)',
      '新機能: 中央ペインのコードブロックに ✎ ボタンを追加。ブロックの中身だけをその場で編集できます(ハイライト・インデント補助・タグ補完つき、保存すると履歴に 1 版残ります)',
      '新機能: テキスト系の添付ファイル(.json / .yaml / .xml / .svg / .css / .js / .txt など)に「✎ 編集」を追加。開いてその場で編集・保存できます(保存すると履歴に 1 版残ります)',
      '改善: 編集を保存したときの引っかかりも減らしました。左ペインの各行は一度作ったものを使い回す仕組みでしたが、データを 1 文字でも保存すると 全行を捨てて作り直していました(履歴の件数や被参照数の表示が古くなる可能性があるため)。これらの表示は行ごとに安く比べられるので、中身が実際に変わった行だけ作り直すようにしました。実測(5000 エントリ)で、保存 1 回あたり画面が固まる時間が 749ms → 531ms。エントリ数が多いほど効果があります',
      '改善: エントリの編集を開くときの引っかかりを減らしました。これまでは編集を開く / 閉じるたびに、左ペインの一覧を全件作り直していました(1000 件なら 1000 行、5000 件なら 5000 行)。一覧の中身は編集中かどうかで変わらないので、作り直さず再利用するようにしています。実測(5000 エントリ)で、保存を伴わない編集操作 1 回あたりの画面が固まる時間が 686ms → 394〜446ms(約 4 割減)。エントリ数が多いほど効果があります',
      '改善: 起動が 1 バイトも書き込まなくなりました。これまでは、何も編集していなくてもアプリを開くたびにデータ全体が保存し直されていました。実測(5000 エントリ・履歴 15000 件)で、起動 1 回あたり 25.7MB → 0。データが大きいほど、また起動のたびに効きます(原因: 読み込み完了そのものが保存のきっかけになっていたため。読み込んだ内容が 1 文字も変わっていない場合だけ書き込みを省きます。設定や表示が更新された場合はこれまでどおり保存されます)',
      '安全策: 本文の読み出しに失敗したとき、その本文を「空」として確定させないようにしました。これまでは読み出せなかった場合も「読み込み済み」として扱われ、そのまま保存すると空の本文で上書きされる可能性がありました。今後は読めなかった本文は未読のまま保持され、保存が保留されます(空で上書きしません)。一時的な失敗であれば、次に開いたときに復元されます',
      '🔴 お知らせ: 省容量形式(lazy_entry_bodies)の提供を終了します。約 3 ヶ月後に廃止予定で、まず本日から新たにオンにできなくなりました。すでにオンにしている場合、次にデータを保存した時点で自動的に従来形式へ戻ります(本文・履歴・添付はそのまま引き継がれます。お手元で何かする必要はありません)。理由: 効果があったのは書き込み量の削減だけで、起動が速くなるという当初の説明は測り直したところ成り立っておらず、一方でこの形式まわりに不具合が集中していたためです(本日 3 件修正しました)。万一、本文が読み出せない状態のときは書き戻しを行わず、元のデータをそのまま保持します',
      '不具合修正: 省容量形式(lazy_entry_bodies)で、圧縮された履歴の読み出しに一時的に失敗したとき(メモリ不足など)、その塊に入っていた編集履歴がまとめて消えてしまう問題を修正しました。読み出せなかった塊はそのまま残し、新しい履歴は別の塊に書くようにしています(データが壊れていたのか一時的な失敗だったのか区別できないため、消さない側に倒しました)',
      '🔴 重要な不具合修正: 省容量形式(Flags Inspector の lazy_entry_bodies)をオンにしている場合、バックアップ ZIP や HTML 書き出しに 添付ファイルが 1 件も含まれないことがある問題を修正しました。起動直後など、本文がまだ読み込まれていない状態で書き出すと発生します(本文だけは入るため、開くまで気づけませんでした)。ストレージ形式を切り替える前に自動生成される「移行前バックアップ ZIP」も同じ経路だったため、安全網そのものが添付なしになっていました。この形式をお使いの場合、過去に取ったバックアップに添付が入っているかご確認ください(データ本体は無事です。取り直しをおすすめします)',
      '改善: 差分保存(differential_save)をオンにしているとき、編集のたびの書き込み量が大幅に減りました。実測(5000 エントリ・履歴 15000 件・関連 3074 件)で 1 編集あたり 1,270KB → 11KB(約 115 分の 1)。本文を 1 文字直しただけでも、エントリ同士の「関連」全件と、エントリ・履歴の並び順の一覧全件が毎回まるごと書き直されていたのが原因です(関連は変わったときだけ書き、並び順は一定件数ごとに区切って末尾だけ書き足すようにしました)。データが大きいほど効果があります',
      '改善: サイドバーでエントリを選ぶたびに、データ全体がまるごと保存し直されていた問題を修正しました。実測(1000 エントリ・履歴 3000 件の 5MB のデータ)で、選択 1 回あたり 5.1MB → 0、編集 1 回あたり 10.2MB → 5.1MB(半減)。データが大きいほど効果があります(原因: 既定オフのタブ機能が、無効のときも開いたタブの状態をデータ本体に書き込んでいたため。タブ機能を有効にしている場合の動作は変わりません)',
      '改善: 編集履歴が多いワークスペースの起動が大幅に速くなりました。実測(15000 エントリ・履歴 45000 件)で 27.2 秒 → 0.9 秒(約 29 倍)。サイドバーの各行で「履歴あり」の印を出すために、1 行ごとに履歴全体を数え直していたのが原因です(エントリ数 × 履歴数に比例して遅くなっていました)。履歴が多いほど効果があります',
      '改善: コードブロックのシンタックスハイライトが xml / svg に対応しました(```xml / ```svg)',
      '不具合修正: 省容量形式(lazy_entry_bodies、旧「高速起動形式」)をオンにしたあと従来形式へ戻したとき、分割・圧縮された本文と履歴の実体がブラウザ内に残り続け、保存領域を食っていた問題を修正しました(次回の保存で自動的に回収されます。データの読み書きには元から影響ありません)',
      '訂正: これまで「高速起動形式」と呼んでいた設定(Flags Inspector の lazy_entry_bodies)を、実測にもとづき「省容量形式」に改称しました。5000 エントリ・履歴 15000 件で測ったところ、保存領域は約 0.6 倍に減る一方、起動は約 1.8 倍遅くなります(履歴を圧縮して持つぶん、起動時の展開に時間がかかるため)。保存領域が足りないとき以外は既定のままをおすすめします。あわせて、この形式は差分保存(differential_save)も同時にオンにしないと働かないことをマニュアルに明記しました。動作そのものは変わっていません',
    ],
  },
  {
    id: '2026-07-24-codeblock-render-wave',
    date: '2026-07-24',
    title: '2026-07-24 更新のお知らせ',
    items: [
      '新標準: コードブロックのレンダリング記法を統一しました。レンダリングできる言語(html / mermaid / csv / tsv / psv)は、無印(```html など)= レンダリング表示 + 右上の ‹/› ボタンでソース切替 / -render = レンダリングのみ / -norender = コードのみ、になります(例: ```html-norender)。従来の ```html-render はそのまま「レンダリングのみ」として動き続けます',
      'ご注意: これまで「コード例」として ```html を使っていたブロックはレンダリング表示に変わります。コードのまま見せたい箇所は ```html-norender に書き換えてください(切替ボタンでソースも見られます)',
      'mermaid 図が既定で描画されるようになりました(設定不要。マニュアル 07 章の図解もそのまま表示されます)。コード表示に固定したい場合は ```mermaid-norender',
      '改善: レンダリング表示(html / mermaid / csv)のブロックにもコピー ⧉ ボタンが付きました。コピーされるのは「今見えている面」です(html / mermaid はソース、csv / tsv / psv は表として ── Excel などにそのまま表で貼れます。‹/› でソース面に切り替えていればソースをコピー)',
      '不具合修正: 表をコピーすると、画面上の飾り(行番号の # 列、並べ替え ↕ / 絞り込み ⌕ の記号)まで一緒に貼り付けられてしまう問題を修正しました(Excel や Word に表がきれいに貼れるようになりました)',
      '安全策: ブラウザ保存が使えない環境の案内ダイアログから「フォルダを選んで続行」した場合も、切替の前に移行前バックアップ ZIP を選んだフォルダへ自動作成するようになりました(バックアップに失敗した場合は切り替えず、ダイアログに戻って選び直せます)。これで Settings からの切替と同じ保護になります',
      '不具合修正: エントリを別ウィンドウで開いたとき、```html-render の埋め込み表示が高さ 0 のまま見えない問題を修正しました',
      '不具合修正: Viewer ポップアップの mermaid 図まわり(枠・ソース・エラー表示)のスタイルが本体画面と揃っていなかった問題を修正しました',
    ],
  },
  {
    id: '2026-07-22-refinement-round',
    date: '2026-07-22',
    title: '2026-07 更新のお知らせ',
    items: [
      '不具合修正: マニュアル(pkc2-manual.html)で一部の章(ファイラビュー等)の画像が表示されない問題を修正しました(GitHub 上の Markdown 版でも全画像が表示されるようになりました)',
      'マニュアル: 「07 保存と持ち出し」章を刷新しました。保存先 3 種(ブラウザ内 / OPFS / 実フォルダ)× 保存領域が使えない環境のフォールバック × 保存形式の全体像を図解で説明し、案内ダイアログの選択肢の使い分け・UI 設定の持ち出し(Settings File)も掲載しています',
      '不具合修正: Firefox で ⚙ Settings メニューのレイアウトが崩れる(列が横にはみ出す)問題を修正しました',
      '改善: 画面設定・お知らせ既読・タブ構成などの UI 設定がデータ本体と一緒に保存されるようになりました(ブラウザの localStorage が毎回消去される環境でも設定が維持されます。バックアップ ZIP / HTML 書き出しにも設定が同乗します)',
      '新機能: ⚙ Settings に「設定エクスポート / 設定インポート」を追加。画面設定・UI 設定だけを小さなファイル(.pkc2-prefs.json)で持ち出し / 持ち込みできます(データ・本文・添付は含まれません。インポートは差分を確認してから適用)',
      '改善: ブラウザ内の保存領域(IndexedDB)が使えない環境では、起動時に説明つきの案内を表示して「フォルダ保存 / 都度保存 / 閲覧のみ」を選べるようにしました(黙って機能低下しません。従来はこの環境で起動自体に失敗することがありました)',
      '改善: 上記の環境で「フォルダを選んで続行」を選ぶと、選んだフォルダへ完全なバックアップ ZIP を自動保存し続けるようになりました(編集のたびに数秒後へまとめて書き込み。ブラウザの仕様上、次回起動時はフォルダの選び直しが必要です)',
      '安全策: ストレージをローカルフォルダへ切り替える際、移行前バックアップ ZIP を移行先フォルダへ自動作成するようになりました(バックアップに失敗した場合は切替を中止します)',
      '安全策: ストレージ形式の切替(Flags Inspector の lazy_entry_bodies)を ON にすると、切替前に完全なバックアップ ZIP を自動生成するようになりました(バックアップに失敗した場合は切替を中止します)',
      '改善: 大きな添付(画面収録など)もサイズに関係なくそのまま表示されるようになりました(「大きなファイルは開く時に読み込み」の制限を撤去。新しい読み込み方式はメモリを消費せず、起動やほかの操作を遅くしません)。ランチャー登録アプリとアイコンは起動直後に先読みされ、クリック時の待ちがなくなります',
      '不具合修正: データが大きい(収録・添付が多い)とエクスポートが「string length」エラーやメモリ不足で失敗しバックアップできない問題を修正(逐次書き出し + 大きな添付は無変換で書き出し)',
      'バックアップは 💾 Backup ZIP を推奨: メニュー最上段に格上げしました(アセット分離形式でデータ量によらず確実。HTML 書き出しは配布・持ち運び用として従来どおり)',
      '改善: 大きな添付(4MB 超)は画面表示では読み込まず、開く / ダウンロード時にだけ読み込むように(巨大な収録がある環境で全体の読み込みが極端に遅くなる問題の対策)。開く / ダウンロードはストレージ直読みで待ち行列を跳ばします',
      '差分保存の既定オンを撤回しました: 大きなデータ + 遅いストレージで起動が極端に遅くなる報告のため(#958)。保存形式は次回の保存時に自動で従来の単一形式へ復元されます。書込頻度を抑えたい場合のみ Flags Inspector からオンにできます',
      '不具合修正: HTML アプリ / URL 添付が一時的に「Light export」表示になり開けないことがある問題を修正(読み込み待ちは「⏳ 読み込み中」表示になり、ボタンはそのまま使えます)',
      '画面収録: 大きい収録は停止時に「ダウンロード保存」を選べるように(長時間収録でタブが落ちる問題の対策)',
      'コマンドパレット: 使えないコマンドがグレー + 理由つき表示に(実行方法も案内)',
      'タブ機能が ⚙ Settings の「Tabs」から有効化できるように。ショートカット一覧に Views & Tabs 節を追加(Alt+1〜6 など)',
      'どこでも右クリックメニューが既定 ON に。ランチャータイルも右クリック対応',
      '確認・入力ダイアログを画面をブロックしないポップオーバーに刷新',
      'カレンダー月送り・サイドバー・検索の体感を高速化',
      'ローカルフォルダ(FSA)モード: 前回のフォルダへワンクリックで再接続できるバナーを追加',
    ],
  },
];

/** オフスイッチ(⚙ Settings の「News」トグル / Flags Inspector)。 */
export const shellStartupNoticeEnabled = defineFlag<boolean>(
  'shell.startup_notice_enabled',
  true,
  {
    category: 'shell',
    description: '起動後にアップデートのお知らせを表示(1 リリースにつき 1 回)。OFF で非表示',
  },
);

const SEEN_KEY = 'pkc2.startup-notice.seen';
const DISABLED_KEY = 'pkc2.startup-notice.disabled';
const REGION = 'startup-notice';

// C11: 既読管理は ui-prefs facade 経由(container バッグ優先 +
// localStorage ミラー)。localStorage が毎回初期化される環境で
// 「起動のたびにお知らせが再表示される」問題を解消する。
const lsGet = getUiPref;
const lsSet = setUiPref;

function isAutomated(): boolean {
  try {
    return (globalThis.navigator as { webdriver?: boolean } | undefined)?.webdriver === true;
  } catch {
    return false;
  }
}

function forceRequested(): boolean {
  try {
    return new URLSearchParams(globalThis.location?.search ?? '').get('pkc-startup-notice-force') === '1';
  } catch {
    return false;
  }
}

/** test 用 ── 既読 / 無効化 state をリセット。 */
export function __resetStartupNoticeForTest(): void {
  removeUiPref(SEEN_KEY);
  removeUiPref(DISABLED_KEY);
  document.querySelector(`[data-pkc-region="${REGION}"]`)?.remove();
}

/**
 * 条件を満たせばお知らせカードを表示する。戻り値は表示したカード
 * (非表示条件に該当したら null)。冪等 ── 既存カードは張り替える。
 */
export function maybeShowStartupNotice(
  dispatcher: Dispatcher,
  host: HTMLElement = document.body,
): HTMLElement | null {
  const latest = STARTUP_NOTICES[0];
  if (!latest) return null;
  if (!shellStartupNoticeEnabled()) return null;
  if (lsGet(DISABLED_KEY) === '1') return null;
  if (lsGet(SEEN_KEY) === latest.id) return null;
  // automation(Playwright 等、navigator.webdriver)では表示しない ──
  // リリースごとに内容・サイズが変わるカードは、既存 90+ smoke spec の
  // 座標 click / elementFromPoint を非決定的に妨げる(kanban DnD で実際に
  // 発生)。お知らせ自体の parity spec だけ URL param で明示解除する。
  if (isAutomated() && !forceRequested()) return null;
  const state = dispatcher.getState();
  if (state.embedded) return null;

  document.querySelector(`[data-pkc-region="${REGION}"]`)?.remove();

  const card = document.createElement('div');
  card.className = 'pkc-startup-notice';
  card.setAttribute('data-pkc-region', REGION);
  card.setAttribute('role', 'status');

  const heading = document.createElement('div');
  heading.className = 'pkc-startup-notice-heading';
  heading.textContent = `📢 ${latest.title}`;
  card.appendChild(heading);

  const list = document.createElement('ul');
  list.className = 'pkc-startup-notice-list';
  for (const item of latest.items) {
    const li = document.createElement('li');
    li.textContent = item;
    list.appendChild(li);
  }
  card.appendChild(list);

  const actions = document.createElement('div');
  actions.className = 'pkc-startup-notice-actions';

  const markSeen = (): void => {
    lsSet(SEEN_KEY, latest.id);
    card.remove();
  };

  const muteBtn = document.createElement('button');
  muteBtn.type = 'button';
  muteBtn.className = 'pkc-btn-small pkc-startup-notice-mute';
  muteBtn.setAttribute('data-pkc-action', 'startup-notice-mute');
  muteBtn.textContent = '今後表示しない';
  muteBtn.addEventListener('click', (e) => {
    e.stopPropagation();
    markSeen();
    // container 永続の flag を OFF に(readonly では SET_FLAG が reducer で
    // 弾かれるため、viewer-local の localStorage を fallback に使う)
    if (dispatcher.getState().readonly) {
      lsSet(DISABLED_KEY, '1');
    } else {
      dispatcher.dispatch({ type: 'SET_FLAG', key: 'shell.startup_notice_enabled', value: false });
    }
  });
  actions.appendChild(muteBtn);

  const closeBtn = document.createElement('button');
  closeBtn.type = 'button';
  closeBtn.className = 'pkc-btn-small pkc-startup-notice-close';
  closeBtn.setAttribute('data-pkc-action', 'startup-notice-close');
  closeBtn.textContent = '閉じる';
  closeBtn.addEventListener('click', (e) => {
    e.stopPropagation();
    markSeen();
  });
  actions.appendChild(closeBtn);

  card.appendChild(actions);
  host.appendChild(card);
  return card;
}

/**
 * boot 経路用: 最初の ready(container あり)を待って 1 回だけ表示を
 * 試みる。boot 直後の描画と競合しないよう少しだけ遅らせる。
 */
export function mountStartupNotice(
  dispatcher: Dispatcher,
  host?: HTMLElement,
  delayMs = 600,
): () => void {
  let fired = false;
  const unsub = dispatcher.onState((s) => {
    if (fired) return;
    if (s.phase !== 'ready' || !s.container) return;
    fired = true;
    setTimeout(() => {
      maybeShowStartupNotice(dispatcher, host);
    }, delayMs);
    queueMicrotask(() => unsub());
  });
  return unsub;
}
