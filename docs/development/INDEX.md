# Development Docs — Issue Index

Last updated: 2026-04-24（Repository Residual Cleanup Phase 2: 残り 3 spec DRAFT の Status を現実に同期、FI-02A 実装済み / FI-02B 再現不可 close / FI-12 実装済み、docs-only、src / UI / tests / bundle / version / About / CHANGELOG いずれも不変）.

## Status Legend

| Status | Meaning |
|--------|---------|
| CLOSED | Implementation complete, tests pass, doc accurate. Historical. |
| CLOSED (omnibus) | Multi-issue doc; all sub-issues complete. |
| COMPLETED | Recently finished. Implementation verified, not yet historical. |
| CANDIDATE | Under consideration for next implementation. |

## CLOSED (42 docs)

All 42 historical docs passed strict close audit (2026-04-11).

| # | File | Topic | Notes |
|---|------|-------|-------|
| 1 | `action-surface-consolidation.md` | Sidebar/header action refactor | |
| 2 | `asset-autocomplete-foundation.md` | `/asset` autocomplete | |
| 3 | `asset-picker-foundation.md` | Asset picker modal | |
| 4 | `asset-reference-resolution.md` | `asset:key` → data URL resolution | |
| 5 | `attachment-preview-strategy.md` | Attachment preview phases 1-3 | |
| 6 | `batch-import-result-feedback.md` | Import result notification | |
| 7 | `batch-import-target-folder-selection.md` | Import target folder selection | |
| 8 | `batch-import-transaction-hardening.md` | Atomic batch import via SYS_APPLY_BATCH_IMPORT | |
| 9 | `container-wide-batch-import.md` | Multi-entry batch import pipeline | Supersession notes added |
| 10 | `critical-input-attachment-recovery.md` | Input loss prevention | |
| 11 | `dnd-cleanup-robustness.md` | DnD state cleanup safety nets | Test coverage section added |
| 12 | `edit-preview-asset-resolution.md` | Edit preview + asset resolution (omnibus) | 9 sub-issues; TOC added |
| 13 | `entry-level-deep-preview.md` | Entry deep preview | |
| 14 | `entry-window-archetype-display.md` | Archetype display in entry window | |
| 15 | `entry-window-preview-phase4.md` | Entry window preview phase 4 | |
| 16 | `folder-scoped-export.md` | Folder-scoped export | |
| 17 | `folder-scoped-import.md` | Folder-scoped import | Supersession notes added |
| 18 | `folder-structure-restore.md` | Folder hierarchy restoration on import | |
| 19 | `import-preview-ui.md` | Import preview UI | |
| 20 | `input-assistance-foundation.md` | Input assistance (calc, auto-list) | |
| 21 | `light-mode-badge-ui.md` | Light mode badge styling | |
| 22 | `markdown-phase2.md` | Markdown rendering phase 2 | |
| 23 | `mixed-container-export.md` | Mixed archetype export | |
| 24 | `multi-select-design.md` | Multi-select design spec | Design doc; impl completed (#46) |
| 25 | `non-image-asset-handling.md` | Non-image asset chip foundation | |
| 26 | `pane-resize-selector-migration.md` | Pane resize selector migration | |
| 27 | `release-builder-commit-stamp.md` | Commit hash in release build | |
| 28 | `selective-import.md` | Selective import (checkbox UI) | |
| 29 | `split-editor-preview-asset-resolution.md` | Split editor asset resolution | |
| 30 | `text-container-wide-export.md` | TEXT container-wide export | |
| 31 | `text-markdown-zip-export.md` | TEXT markdown ZIP export | |
| 32 | `text-textlog-editing-ux-consolidation.md` | TEXT/TEXTLOG editing UX consolidation | |
| 33 | `textlog-container-wide-export.md` | TEXTLOG container-wide export | |
| 34 | `textlog-csv-zip-export.md` | TEXTLOG CSV/ZIP export | |
| 35 | `textlog-double-click-edit-review.md` | TEXTLOG double-click edit | |
| 36 | `textlog-foundation.md` | TEXTLOG archetype foundation | |
| 37 | `textlog-polish.md` | TEXTLOG UX polish | Hint text fix applied |
| 38 | `textlog-text-attachment-ux-polish.md` | TEXT/TEXTLOG attachment UX polish | |
| 39 | `todo-cross-view-move-strategy.md` | Todo cross-view DnD strategy | |
| 40 | `todo-layering-fix.md` | Todo layer violation fix | |
| 41 | `todo-view-consistency.md` | Todo view consistency (detail/calendar/kanban) | |
| 42 | `ux-regression-recovery.md` | UX regression recovery | |

## COMPLETED

| # | File | Topic | Completed | Summary |
|---|------|-------|-----------|---------|
| 43 | `markdown-interactive-task-lists.md` | Interactive task list checkbox toggle | 2026-04-11 | TEXT/TEXTLOG の rendered markdown 内 `- [ ]`/`- [x]` を click で toggle。pure helper + QUICK_UPDATE_ENTRY。テスト 38 件。 |
| 44 | `non-image-inline-preview.md` | Non-image inline preview (PDF/audio/video) | 2026-04-11 | TEXT/TEXTLOG body 内の非画像 asset chip を inline preview に展開。`populateInlineAssetPreviews()` + 既存 blob lifecycle 再利用。CSP fallback 対応。テスト 16 件。 |
| 45 | `attachment-sandbox-phase5.md` | Container default sandbox policy | 2026-04-11 | `ContainerMeta.sandbox_policy` (strict/relaxed)。per-entry override 優先の fallback chain。meta pane に select UI。テスト 11 件。 |
| 46 | `calendar-kanban-multi-select-phasing.md` | Calendar/Kanban multi-select (Phase 1 + 2-A/B/C/E + ghost) | 2026-04-11 | Phase 1: visual feedback。Phase 2-A: BULK_SET_STATUS。Phase 2-B: BULK_SET_DATE (set + clear)。Phase 2-C: multi-DnD (Kanban/Calendar/cross-view)。Phase 2-E: Escape clear。Drag ghost: N件バッジ。テスト 76 件追加。残: 2-D (表示順)。 |
| 47 | `multi-dnd-drag-ghost-ux.md` | Multi-DnD drag ghost (N件バッジ) | 2026-04-11 | multi-drag 時に setDragImage で件数バッジ表示。Kanban/Calendar 共通。テスト 9 件。 |
| 48 | `keyboard-navigation-phase1.md` | Keyboard navigation Phase 1 (Arrow Up/Down) | 2026-04-11 | Sidebar で Arrow Up/Down によるエントリ移動。SELECT_ENTRY 再利用。container lid 検証で stale DOM 防御。テスト 15 件。 |
| 49 | `stale-listener-prevention.md` | Stale listener prevention (test infra) | 2026-04-11 | テスト間の dispatcher onState/onEvent リスナー漏れを auto-tracking wrapper で解消。本番コード変更なし。テスト 2 件追加。 |
| 50 | `keyboard-navigation-phase2-enter.md` | Keyboard navigation Phase 2 (Enter) | 2026-04-11 | Enter で選択中エントリの編集開始。既存 BEGIN_EDIT 再利用。reducer/renderer 変更なし。テスト 12 件。 |
| 51 | `keyboard-navigation-phase3-tree.md` | Keyboard navigation Phase 3 (Arrow Left/Right) | 2026-04-11 | Arrow Left/Right で folder の折りたたみ/展開。TOGGLE_FOLDER_COLLAPSE 再利用。sidebar 限定。テスト 18 件。 |
| 52 | `keyboard-navigation-phase4-parent.md` | Keyboard navigation Phase 4 (Arrow Left → parent) | 2026-04-11 | collapsed folder で Arrow Left → 親フォルダ選択。getStructuralParent 再利用。テスト 15 件。 |
| 53 | `keyboard-navigation-phase5-child.md` | Keyboard navigation Phase 5 (Arrow Right → child) | 2026-04-11 | expanded folder で Arrow Right → 最初の子を選択。getFirstStructuralChild 新規追加。テスト 16 件。 |
| 54 | `keyboard-navigation-phase6-nonfolder-parent.md` | Keyboard navigation Phase 6 (non-folder Left → parent) | 2026-04-11 | non-folder entry で Arrow Left → 親フォルダ選択。archetype guard 緩和のみ。テスト 15 件。 |
| 55 | `calendar-kanban-keyboard-navigation.md` | Kanban keyboard Phase 1 (Arrow navigation) | 2026-04-11 | Kanban view で Arrow Up/Down (列内) + Left/Right (列間) navigation。viewMode 分岐で sidebar 不変。テスト 24 件。 |
| 56 | `calendar-kanban-keyboard-navigation.md` | Calendar keyboard Phase 1 (Arrow navigation) | 2026-04-11 | Calendar view で Arrow Left/Right (日移動) + Up/Down (週移動)。空セル/空週スキップ。月境界 no-op。テスト 24 件。 |
| 57 | `calendar-kanban-keyboard-navigation.md` | Kanban keyboard Phase 2 (Space status toggle) | 2026-04-11 | Kanban view で Space → todo status toggle。QUICK_UPDATE_ENTRY 再利用。multi-select 非対応。テスト 15 件。 |
| 58 | `entry-window-interactive-task-toggle.md` | Entry window interactive task toggle | 2026-04-11 | entry window 内 task checkbox を click → 親 `QUICK_UPDATE_ENTRY` → `pushViewBodyUpdate` で反映。TEXTLOG は per-log-entry 描画 + `data-pkc-log-id` で識別。readonly CSS guard。protocol 追加: `pkc-entry-task-toggle`。テスト 16 件。 |
| 59 | `task-completion-badge.md` | Task completion badge (sidebar + detail pane) | 2026-04-11 | TEXT/TEXTLOG の task list 進捗を sidebar + detail pane に `done/total` badge 表示。`countTaskProgress()` pure helper。TEXTLOG は全 log entry 合算。task 0 件は非表示。全完了は success 色。テスト 26 件。 |
| 60 | `kanban-keyboard-phase3-ctrl-arrow.md` | Kanban keyboard Phase 3 (Ctrl+Arrow status move) | 2026-04-11 | Kanban view で Ctrl+Arrow Left/Right により todo status を列方向に変更。KANBAN_COLUMNS 参照。QUICK_UPDATE_ENTRY 再利用。single selection のみ。テスト 18 件。 |
| 61 | `entry-window-task-completion-badge.md` | Entry window task completion badge | 2026-04-12 | entry window view title row に `done/total` badge 追加。child 側で `#body-view` DOM から `.pkc-task-checkbox` をカウントして導出。protocol 変更なし。parent 側変更なし。4 経路で同期（init/push/save/flush）。テスト 16 件。 |
| 62 | `entry-window-structured-editor-parity.md` | Entry window structured editor parity | 2026-04-12 | TEXTLOG/todo/form の entry window 編集を raw JSON → presenter ベース構造化エディタに修正。`syncDomPropertiesToHtml()` で outerHTML シリアライズ前の DOM property→HTML 同期契約を確立。TEXTLOG save 後 per-log-entry 再描画修正。テスト 19+11 件。 |
| 63 | `ui-readability-and-editor-sizing-hardening.md` | UI readability & editor sizing hardening (Slices A + B + C) | 2026-04-12 | Slice B: PDF / HTML sandbox preview に `position: relative; z-index: 10000` で CRT scanline overlay を除外。Slice C: center pane textarea `rows = max(15, lineCount+3)` + entry window 非構造化 textarea を `data-pkc-viewport-sized` + `data-pkc-wide` で viewport 追従。Slice A: `:root` に `--font-body` / `--radius-sm` / `--c-text` / `--c-text-dim` を定義、`.pkc-md-rendered` line-height 1.45、`pre` 1.35。テスト 10+4+6+11 = 31 件。 |
| 64 | `orphan-asset-auto-gc.md` | Orphan asset auto-GC on container-replacement paths (Tier 2-1) | 2026-04-14 | `removeOrphanAssets` を `SYS_IMPORT_COMPLETE` (reduceReady / reduceError) + `CONFIRM_IMPORT` の 3 経路に wiring。0 件時は identity 維持で既存 integration test を壊さない。非対象経路（DELETE_ENTRY/COMMIT_EDIT/QUICK_UPDATE_ENTRY/BULK_DELETE）は revision restore との整合のため手動掃除に据え置き。テスト 8 件追加。 |
| 65 | `bulk-restore-ui.md` | Bulk restore UI for BULK_* revisions (Tier 2-2) | 2026-04-14 | `data-pkc-action="restore-bulk"` + `data-pkc-bulk-id` ボタンを meta pane の History と trash panel に追加。action-binder が `getRevisionsByBulkId` で解決 → confirm → N 件の `RESTORE_ENTRY` dispatch（partial-success semantic）。新 reducer action 追加なし。テスト 14 件追加。 |
| 66 | `../spec/merge-import-conflict-resolution.md` | Merge import conflict resolution — design spec (Tier 2-3, docs-only) | 2026-04-14 | 複数 container の merge import に関する設計仕様を `docs/spec/` に追加。衝突軸 5 種（entry/asset/relation/revision/metadata）を整理、3 案（A: Overlay / B: Policy-driven / C: Staging）を比較して **Option A** を MVP 採用。`features/import/merge-planner.ts` の pure helper と `CONFIRM_MERGE_IMPORT` 1 action 追加で完結。実装・テスト変更なし。data-model.md §11.7.4 / §14.6 / §15.5 に相互リンク追加。 |
| 67 | `merge-import-implementation.md` | Merge import Overlay MVP impl (Tier 3-1) | 2026-04-14 | `features/import/merge-planner.ts`（pure）+ `CONFIRM_MERGE_IMPORT` / `SET_IMPORT_MODE` reducer cases + `CONTAINER_MERGED` event + preview UI の mode radio + 5 行 merge サマリ + orphan auto-GC の merge 経路拡張。`AppState.importMode?: 'replace'\|'merge'` を optional で追加（既存 AppState リテラル fixture の regression 回避）。I-Merge1 / I-Merge2 を厳守。テスト 29 件追加（planner 13、reducer+integration+UI 16）。`data-model.md §14.6` に I-IO1b を追加。 |
| 68 | `release-automation-and-smoke-baseline.md` | Tier 3-2 — release automation + bundle size budget + Playwright smoke baseline | 2026-04-14 | `.github/workflows/release.yml`（`v*` tag push で GitHub Release 自動作成、`dist/pkc2.html` + `PKC2-Extensions/pkc2-manual.html` を artifact 添付、prerelease 判定あり）。`build/check-bundle-size.cjs` + ci.yml に hard-fail budget（bundle.js 615 KB / bundle.css 90 KB、現状 79.8% / 78.5%）。`tests/smoke/app-launch.spec.ts` 1 本（boot → Text create → editing phase）を `.github/workflows/smoke.yml` で push-to-main / PR-to-main 時に実行。自前 static server `scripts/smoke-serve.cjs` で `http-server` の readiness race を回避。production code は 1 行も触らない。 |
| 69 | `lint-baseline-realignment.md` | Tier 3-3 — lint baseline realignment + CI blocking 化 | 2026-04-14 | `.eslintrc.cjs` を CLAUDE.md §Architecture に整合。adapter→features 禁止ルール撤去（83 errors 解消）。features→adapter / runtime→(anything) 禁止ルール追加で forward drift を pin。`no-unused-vars` に `varsIgnorePattern: '^_'` で 6 errors 解消。9 件の test 内 `any` warning を `eslint-disable-next-line` + reason で止血。`ci.yml` の lint step から `continue-on-error: true` を削除 → blocking に昇格。`merge-planner.ts` の `while (true)` → `for (;;)` に変換（意味同一）。production code logic 変更 0、既存 3607 tests + smoke 1 件全 pass。HANDOVER §6.8 を "解消済み" に更新。 |
| 70 | `markdown-extensions/markdown-code-block-highlighting.md` | B-2 — fenced code block syntax highlight (retroactive 反映) | 2026-04-13 (実装) / 2026-04-14 (status 整合) | commit `92921ec` で実装済み。pure features tokenizer（sticky-regex walker、~3 KB gzipped）+ 9 言語（ts/js/json/html/css/bash/yaml/diff/sql/powershell）+ 主要 alias。markdown-it `highlight:` hook 経由で TEXT / TEXTLOG / preview / entry-window / export HTML の全 5 経路に伝播。`--c-tok-*` カラー変数で dark/light theme override。テスト 18 件（features 層）。本セッション（2026-04-14）で B-2 が「待機」のままだった ledger / dev doc / INDEX を整合させ、A-2 × B-2 統合テスト 1 件を `tests/adapter/entry-window-syntax-highlight.test.ts` に追加（split editor preview が highlight を乗せることを pin）。 |
| 71 | `markdown-extensions/markdown-csv-table-extension.md` | B-1 — CSV / TSV fenced block → `<table>` (USER_REQUEST_LEDGER S-16) | 2026-04-14 | `features/markdown/csv-table.ts` に RFC 4180 subset parser（quote / 内部 delimiter / 内部 newline / doubled-quote / CRLF / 空入力フォールバック）+ rectangular HTML renderer + `renderCsvFence`。`markdown-render.ts` の `md.renderer.rules.fence` を上書きして CSV lang 検出時のみ短絡、それ以外は default fence renderer に委譲（B-2 syntax highlight 経路を温存）。`csv` / `tsv` / `psv` + `noheader` flag 対応。XSS 安全（cell 内 HTML escape）。`.pkc-md-rendered table` の既存 CSS を再利用、追加クラス `pkc-md-rendered-csv` で CSV 由来 table を識別。テスト 24（pure parser）+ 8（markdown-it integration、pipe-table 非 regression / B-2 fallback / XSS escape / quoted+newline+escape / empty fallback）。S-15 search-mark とは text 節レベルで自然合成。 |
| 72 | `markdown-extensions/markdown-quote-input-assist.md` | B-3 Slice α — `> X` 行で Enter → `\n> ` 自動継続（USER_REQUEST_LEDGER S-17、PARTIALLY COMPLETED） | 2026-04-14 | `features/markdown/quote-assist.ts` に pure helper `computeQuoteAssistOnEnter(value, caretPos)`、`action-binder.ts` の handleKeydown 内 Enter 分岐で wire（inline-calc 直後 / Ctrl+Enter 直前）。`isSlashEligible` で markdown 対象 textarea に限定、`execCommand('insertText')` で undo stack 保護 + fallback 経路。fall-through: mid-line Enter / 空 `> ` 行 / 非引用行 / Shift・Ctrl・Alt / IME composition / non-collapsed selection。テスト 12（pure）+ 9（handler integration）。残り Slice β（empty `> ` で exit）/ γ（選択範囲 prefix toggle shortcut）/ entry-window 同期は CONDITIONAL のまま。 |
| 73 | `search-ux-partial-reach.md` | A-4 FULL — 検索 UX 実用完成（USER_REQUEST_LEDGER S-18） | 2026-04-14 | supervisor 判断で Slice 刻みをやめ 1 テーマで完成。`features/search/sub-location-search.ts`（TEXT 見出し単位 / TEXTLOG log 単位 / 他 archetype 無視、fenced skip、dedup、maxPerEntry cap）+ 新 user action `NAVIGATE_TO_LOCATION { lid, subId, ticket }` + `AppState.pendingNav` + `adapter/ui/location-nav.ts` の `createLocationNavTracker`（ticket gate + scroll + `.pkc-location-highlight` 1.6s flash）+ main.ts onState 末尾で `tracker.consume`。renderer sidebar flat-mode で `.pkc-entry-subloc` 行 emit、action-binder に `navigate-to-location` case + 単調増加 ticket カウンタ。S-15 `<mark>` と合成、B-2 / A-2 / S-17 と非衝突。テスト +46（pure 21 / nav helper 15 / e2e 10）。 |
| 74 | `pane-state-persistence.md` | H-7 — pane collapse/expand を localStorage で永続化（USER_REQUEST_LEDGER S-19、自主運転モード第1号） | 2026-04-14 | `adapter/platform/pane-prefs.ts`（storage key `pkc2.panePrefs`、in-memory cache、invalid JSON / no-storage fallback）+ `adapter/ui/pane-apply.ts`（DOM apply 共有ヘルパ）+ renderer が shell 組み立て時に `loadPanePrefs()` を読んで `data-pkc-collapsed` を即セット（flash なし）+ `togglePane` が `setPaneCollapsed` + `applyOnePaneCollapsedToDOM` 経由でクリック・shortcut・tray どの経路でも保存。reducer / AppState / user-action への touch 0、HANDOVER §6.2 を「解消済み」に更新。テスト +27（storage 12 / apply 6 / e2e 9、既存 pane-toggle-shortcut test は beforeEach に cache+localStorage clear を追加）。 |
| 75 | `textlog-csv-zip-export.md` | H-4 — textlog CSV `flags` 列追加で forward-compat round-trip（USER_REQUEST_LEDGER S-20、自主運転モード第2号） | 2026-04-14 | `features/textlog/textlog-csv.ts` に `flags` 列を末尾追加 + `KNOWN_TEXTLOG_FLAGS` allow-list + `parseFlagsField` helper。serializer は `important` と `flags` を両方出力（backward-compat）、parser は header に `flags` 列がある場合それを正本にし、無ければ `important` に fallback。unknown token は silent drop（forward-compat）、dedup + 大小文字・空白許容。`TextlogFlag` 拡張時も modern × modern round-trip は lossless に。data model / reducer / UI への touch 0。spec `body-formats.md §3.6.1` を「lossy → modern × modern lossless、legacy 互換維持」に更新。dev doc column contract の row 8 追加。テスト +12（serializer 3 / precedence 4 / backward compat 2 / tolerant parse 3）。HANDOVER §5.7 を「解消済み」に更新。 |
| 76 | `../spec/schema-migration-policy.md` | H-3 — schema_version migration path 設計正本化（USER_REQUEST_LEDGER S-21、自主運転モード第3号、docs-only） | 2026-04-15 | `data-model.md §15.3` の「未設計」を解消。新規 canonical spec を `docs/spec/` に追加。判断基準（additive vs breaking flow）、lazy / eager 適用（JSON 内部は lazy / IDB store 境界は eager）、11 の migration hook 経路（IDB load / `onupgradeneeded` / HTML Full / Light import / ZIP import / bundle import / merge-planner / exporter / transport profile / fixture helper）、canonical entry point `src/core/migrations/migrate-container.ts`（将来配置）、test 戦略 4 系列（unit / chain / round-trip / reject）、v2 到達時の実装順序 9 step を固定。production code touch 0、`SCHEMA_VERSION` 依然 `1`。`data-model.md §15.3 / §17` / `body-formats.md §14.2` / `merge-import-conflict-resolution.md §8.6` / `HANDOVER_FINAL.md §7.3 / §17` に cross-link 追加。テスト追加なし。 |
| 77 | `data-model/revision-branch-restore.md` | H-6 — Revision.prev_rid / content_hash の optional 追加（USER_REQUEST_LEDGER S-22、自主運転モード第4号、記録面のみ） | 2026-04-15 | `Revision` に 2 additive optional field を追加（`prev_rid?` = 同 entry_lid の直前 revision id / `content_hash?` = snapshot の FNV-1a-64 16-char lowercase hex digest）。`src/core/operations/hash.ts` を新規追加し pure BigInt 実装（UTF-8 正規化、astral-plane surrogate pair 対応）、`snapshotEntry` が両 field を populate（旧 rev は absent 維持、lazy 補填なし）。`parseRevisionSnapshot` / `restoreEntry` / `restoreDeletedEntry` は両 field を**読まない**（non-intrusive）ため restore 契約は完全維持。reducer / UI / user-action / schema_version touch 0。spec `data-model.md §6.1 / §6.2 / §6.2.1 / §15.2 / §15.5` 更新、`schema-migration-policy.md §6` lazy 既存例追加、`HANDOVER_FINAL.md §5.8` に 2026-04-15 追記。テスト +22（hash 7 / content_hash 4 / prev_rid 5 / backward compat 3 / round-trip 1 / restore integration 2）、既存 3781 tests 全 pass。C-1 revision-branch-restore 実装時の記録面の下地。 |
| 78 | `../spec/text-textlog-provenance.md` | H-8 — TEXT ↔ TEXTLOG 変換の非可逆境界と来歴設計（USER_REQUEST_LEDGER S-23、自主運転モード第5号、docs-only） | 2026-04-16 | `HANDOVER_FINAL.md §6.3` の「非可逆部分の未解消課題」を canonical spec として固定。TEXT→TEXTLOG / TEXTLOG→TEXT 非可逆境界を表形式で全項目網羅、許容損失の理由付け、`RelationKind = 'provenance'`（additive）の設計根拠・後方互換性、`Relation.metadata?: Record<string,string>`（additive）追加仕様、provenance ペイロード定義（`conversion_kind / split_mode / source_content_hash / converted_at / segment_count / selected_log_count`）、実装スライス A–D（A=本ドキュメント / B=RelationKind 追加 / C=metadata? 追加 / D=変換関数拡張）の順序と依存、テスト戦略スニペット、スキーマ互換性（全変更が v1 範囲内・SCHEMA_VERSION 更新不要）を記述。`data-model.md §5` RelationKind / Relation schema 更新（`provenance` 行追加 / `metadata?` フィールド行追加）、`textlog-text-conversion.md` 末尾に cross-link、`HANDOVER_FINAL.md §6.3` に解消マーカー追記。production code touch 0。テスト追加なし（Slice B–D 実装時に追加予定）。 |

### COMPLETED — Relations / References / Backlinks wave（2026-04-19〜20）

| # | File | Topic | Completed | Summary |
|---|------|-------|-----------|---------|
| 79 | `backlinks-panel-v1.md` | Backlinks Panel v1 — relations 別 sub-panel | 2026-04-19 | meta pane に relations 由来の backlinks を kind 別に表示する panel を v1 で追加。追加 reducer なし（pure derivation）、renderer のみ。以後 References umbrella / sidebar badge / badge-jump / relation delete UI / relation kind edit の起点 |
| 80 | `sidebar-backlink-badge-v1.md` | Sidebar backlink count badge | 2026-04-20 | sidebar の entry 行に relations 由来の inbound 件数 badge を表示。件数 0 は非表示。pure count helper + renderer のみ、action 追加なし |
| 81 | `backlink-badge-jump-v1.md` | Sidebar backlink badge → click jump | 2026-04-20 | badge クリックで meta pane の References / relations セクションへ scroll + flash。既存 `SELECT_ENTRY` 再利用、`data-pkc-action` delegation で実現 |
| 82 | `relation-delete-ui-v1.md` | Relation delete UI | 2026-04-20 | relation 行 × ボタンで 1 本ずつ削除。confirm + `DELETE_RELATION` 追加。undo は非対象、provenance 二重ガードは kind 編集側で |
| 83 | `relation-kind-edit-v1.md` | Relation kind inline edit | 2026-04-20 | relation 行の kind select で inline edit。`provenance` 行の保護は二重ガード（edit 禁止 + 削除 confirm 強化） |
| 84 | `unified-backlinks-v0-draft.md` | Unified Backlinks v0 — design draft | 2026-04-20 | 5 案比較と Option E（References umbrella）採用理由の docs-only draft。v1 で consumed |
| 85 | `unified-backlinks-v1.md` | Unified Backlinks v1 — References umbrella (Option E) | 2026-04-20 | relations / body refs / provenance を References として統一表示する umbrella。summary row v2 / clickable v3 の基盤 |
| 86 | `references-summary-row-v2.md` | References summary row v2 | 2026-04-20 | References header に件数サマリ行を追加（canonical count 契約）。clickable v3 がそのまま継承 |
| 87 | `references-summary-clickable-v3.md` | References summary clickable v3 | 2026-04-20 | サマリ行を clickable にして該当 section へ scroll + flash。sub-location-search の `NAVIGATE_TO_LOCATION` と非衝突 |

### COMPLETED — Provenance metadata wave（2026-04-20）

| # | File | Topic | Completed | Summary |
|---|------|-------|-----------|---------|
| 88 | `provenance-metadata-viewer-v1.md` | Provenance metadata viewer v1 | 2026-04-20 | `provenance` relation の `metadata` を meta pane の References 内で参照可能に。raw display + key 一覧 |
| 89 | `provenance-metadata-pretty-print-v1.md` | Provenance metadata pretty-print v1.x | 2026-04-20 | key scoped formatter（`converted_at` / `source_content_hash` / `segment_count` 等）で human-readable 表示。raw 切替可 |
| 90 | `provenance-metadata-copy-export-v1.md` | Provenance metadata copy / export v1 | 2026-04-20 | raw canonical JSON の copy（clipboard）+ export（`.json` download）。per-field copy / multi-relation bulk は scope 外で明示 |

### COMPLETED — Unified Orphan Detection v3 / Connectedness wave（2026-04-20）

| # | File | Topic | Completed | Summary |
|---|------|-------|-----------|---------|
| 91 | `unified-orphan-detection-v3-draft.md` | Unified Orphan Detection v3+ — design draft | 2026-04-20 | "fully unconnected" の定義候補の比較 + S1..S5 rollout 設計（docs-only）。contract に consumed |
| 92 | `unified-orphan-detection-v3-contract.md` | Unified Orphan Detection v3 — behavior contract | 2026-04-20 | "any relation inbound/outbound + body ref 到達性" を fully unconnected 判定に。S3 / S4 実装の契約、S5 filter は Defer（§7.4） |
| 93 | `connectedness-s3-v1.md` | Connectedness S3 — `buildConnectednessSets` | 2026-04-20 | pure helper。relation + body ref を横断的に連結成分として算出（S4 以降の基盤）。`isMarkdownEvaluatedArchetype` は後に inline 化（`dead-code-inventory-after-relations-wave.md §3.1` 参照） |
| 94 | `connectedness-s4-v1.md` | Connectedness S4 — sidebar fully-unconnected marker | 2026-04-20 | sidebar で fully-unconnected entry を marker 表示。S3 sets を消費、reducer 変更なし |
| 95 | `orphan-detection-ui-v1.md` | Orphan detection UI v1 | 2026-04-20 | relations ベースの orphan 表示 UI v1（v3 の fully-unconnected marker とは別軸、両立） |

### COMPLETED — Dead-code / dead-path maintenance wave（2026-04-19〜21）

| # | File | Topic | Completed | Summary |
|---|------|-------|-----------|---------|
| 96 | `dead-path-cleanup-inventory-01.md` | Dead-path cleanup round 1 | 2026-04-19 | 未使用 export / pseudo-API / spec-only シンボルの audit（core 寄り）。Resolution 表が正本 |
| 97 | `dead-path-cleanup-inventory-02-adapter-ui.md` | Dead-path cleanup round 2（adapter/ui） | 2026-04-19 | adapter/ui 層の audit。A-class 不在、follow-up は intentional defer |
| 98 | `dead-path-cleanup-inventory-03-features.md` | Dead-path cleanup round 3（features） | 2026-04-19 | features 層 audit。`isValidEntryRef` は仕様宣言のため保持、round 4 に継承 |
| 99 | `dead-path-cleanup-inventory-04-platform-markdown-textlog-container.md` | Dead-path cleanup round 4 | 2026-04-19 | platform / markdown / textlog / container 層 audit。PR #40（slugify 重複解消）/ PR #41（`updateLogEntry` 削除）を実施 |
| 100 | `dead-path-cleanup-inventory-05-round5.md` | Dead-path cleanup round 5 | 2026-04-19 | round 5 audit。PR #44（features barrel 削除）/ PR #46（calendar month helper）/ PR #47（record:reject 整合）を実施 |
| 101 | `dead-path-decision-features-barrel.md` | Decision — features barrel 削除 | 2026-04-19 | barrel 削除と `entryMatchesQuery` 保持を分離した決定ログ。PR #44 で執行済み |
| 102 | `dead-path-decision-isUlid-updateLogEntry.md` | Decision — isUlid / updateLogEntry | 2026-04-19 | `isUlid` 保持 / `updateLogEntry` 削除の決定ログ。PR #41 で `updateLogEntry` 削除済み、`isUlid` は C（spec 宣言）として保持 |
| 103 | `dead-code-inventory-after-relations-wave.md` | Dead-code inventory after relations wave | 2026-04-21 | relations wave 後の未使用 export 監査。Category A 2 件は同 PR 内で解消（`isMarkdownEvaluatedArchetype` inline 化 + 1 件削除）。Category B/C/D 空 |

### COMPLETED — P1–P5 wave（2026-04-21）

| # | File | Topic | Completed | Summary |
|---|------|-------|-----------|---------|
| 104 | `recent-entries-pane-v1.md` | P1 — Recent Entries Pane v1 | 2026-04-21 | `created_at` desc の派生ビューを sidebar 上段に開閉可能な `<details>` として追加。container 変更 / reducer 変更 / 新 action いずれも無し。S4 orphan marker と対の UX |
| 105 | `breadcrumb-path-trail-v1.md` | P2 — Breadcrumb / Path Trail v1 | 2026-04-21 | 既存 breadcrumb 実装を spec 化し、軽微 hardening を追加。segment click で `SELECT_ENTRY`、sidebar scroll 不要な迷子補修 |
| 106 | `entry-rename-freshness-audit.md` | P3 — Entry Rename Freshness Audit | 2026-04-21 | rename 直後の表示 freshness 監査（docs-only）。stale surface は 1 件（entry-window title）のみ。follow-up は #107 で実装 |
| 107 | `entry-window-title-live-refresh-v1.md` | P3 follow-up — Entry-window title live refresh v1 | 2026-04-21 | entry-window の title を親 entry の rename に追従。既存 `QUICK_UPDATE_ENTRY` 経路再利用、archetype label など v1 非対象は §6 で明示 |
| 108 | `saved-searches-v1.md` | P4 — Saved Searches v1 | 2026-04-21 | filter / query / sort 組み合わせを named slot に保存 / 復元。localStorage 永続化、container 契約は非変更。rename / pin / date-range 等は §9 で v1+ 明示 |
| 109 | `extension-capture-v0-draft.md` | P5 — Extension Capture v0 draft | 2026-04-21 | 外部 extension → PKC2 capture の設計 draft（docs-only）。`record:offer` 再利用 Option B を推奨、次 PR は `docs/spec/record-offer-capture-profile.md` 策定。実装はまだ |
| 110 | `next-feature-prioritization-after-relations-wave.md` | Next-feature prioritization memo | 2026-04-21 | P1–P5 軸の棚卸し docs-only memo。P1–P4 + P3 follow-up は本 wave で shipped、P5 のみ draft 段階 |

### COMPLETED — Hook subscription / Transport record wave（2026-04-19〜20）

| # | File | Topic | Completed | Summary |
|---|------|-------|-----------|---------|
| 111 | `transport-record-accept-reject-consistency-review.md` | Transport `record:accept` / `record:reject` consistency review | 2026-04-19 | 送受非対称の整合性レビュー。PR #45 / #47 で解消 |
| 112 | `transport-record-reject-decision.md` | Transport `record:reject` — sender-only decision | 2026-04-19 | sender-only 方針を固定。関連 `capability.ts` / tests に反映 |
| 113 | `pkc-message-hook-subscription-review.md` | Hook subscription — design review | 2026-04-20 | docs-only、論点整理。Defer 決定に supersede |
| 114 | `pkc-message-hook-subscription-poc.md` | Hook subscription — PoC design | 2026-04-20 | docs-only、PoC 設計。Defer 期間中は凍結保存 |
| 115 | `pkc-message-hook-subscription-acceptance.md` | Hook subscription — acceptance contract | 2026-04-20 | docs-only、acceptance 仕様。Defer 下で据え置き |
| 116 | `pkc-message-hook-subscription-decision.md` | Hook subscription — Go/No-Go decision（**canonical entry point**） | 2026-04-20 | **結論: Defer**。実装前に simpler proof path（polling 等）を通す方針。以後、hook subscription の canonical reference はこの doc |

### COMPLETED — UI continuity wave（2026-04-22）

ユーザ報告 7 件を起点にした docs-first investigation × 最小 PR × 複数本 の連鎖。`HANDOVER_FINAL.md §23` で closure record。

| # | File / PR | Topic | Completed | Summary |
|---|---|---|---|---|
| 117 | PR #99 | Cluster B: scroll-preservation helper | 2026-04-22 | `preserveCenterPaneScroll(mutate)` を `bindActions` local に 1 本追加、`toggle-todo-status` / `toggle-sandbox-attr` / `toggle-log-flag` を統合。TEXTLOG checkbox / HTML 許可 checkbox の上戻りを解消 |
| 118 | PR #100 | Cluster C: recent pane collapse state | 2026-04-22 | `AppState.recentPaneCollapsed?: boolean` + `TOGGLE_RECENT_PANE` action + renderer state 駆動化。Recent pane を畳んでも次 render で復活する現象を解消 |
| 119 | PR #101 | Cluster A: state-driven storage profile overlay | 2026-04-22 | `AppState.storageProfileOpen?: boolean` + `OPEN/CLOSE_STORAGE_PROFILE` actions + renderer 所有化。`root.innerHTML = ''` による即消滅を解消。メニュー全体の event wiring 主因も同 PR で除去 |
| 120 | PR #102 | Cluster E (low-risk optimization): shared `LinkIndex` per render | 2026-04-22 | `buildConnectednessSets(container, linkIndex?)` に optional 引数追加、renderShell で 1 回計算して sidebar / meta で共有。compute 半減、bundle +0.05 kB |
| 121 | PR #103 | Cluster C' first wave: opt-in reveal policy | 2026-04-22 | `SELECT_ENTRY` / `NAVIGATE_TO_LOCATION` に `revealInSidebar?: boolean` を additive 追加、reducer を opt-in gate、reveal 必須 2 経路（storage-profile / entry-ref）のみ明示 opt-in |
| 122 | PR #104 | Cluster C' follow-up: reveal policy lockdown | 2026-04-22 | 残り 6 経路（recent pane / calendar kb nav / kanban kb nav / calendar drop / kanban drop / relative folder nav）の reveal 不要を決定理由コメント + 回帰テストで固定。実コード不変、bundle size 変化なし |
| 123 | PR #105 | Cluster D first slice: child window shortcut bridge | 2026-04-22 | child window inline script に keydown listener を追加、Ctrl+S → 既存 `pkc-entry-save` path、Escape → `cancelEdit()` / `window.close()`。parent 側は完全に不変 |
| 124 | `storage-profile-footprint-scope.md` | Storage Profile — asset-only vs full container footprint の scope 固定（docs-only） | 2026-04-22 | 現状 Storage Profile が `container.assets` のみを集計する asset-only profile であることと、ユーザが期待していた "text body / relations / revisions を含む full container footprint" が未実装かつ別概念であることを正本化。Slice A(UI label clarification) / B(body bytes column) / C(relations & revisions) / D(persisted vs export size) / E(manual & doc update) の additive 実装候補を整理、`storage-profile-ui.md` 側にも冒頭 scope note を cross-link 追加。実コード変更なし |
| 125 | `child-window-shortcut-parity-status.md` | Child window shortcut parity — 残件監査の正本化（docs-lite） | 2026-04-22 | PR-ζ₁(#105, Ctrl/Cmd+S + Escape) / PR-ζ₂(date/time 6 shortcut) の後、残る main-shell shortcut を **parity / intentional non-parity / backlog** の 3 bucket に棚卸し。`mod+?` / pane toggle / `mod+N` / `mod+Enter` / arrow / Enter / `/` / asset picker 系は child の UI 不在により意図的 non-parity、`mod+Z/Y` は親子共通 backlog。audit の結果 tiny omission は無し、コード変更なし |
| 126 | `../spec/tag-color-tag-relation-separation.md` | W1: Tag / Color tag / Relation 概念分離（docs-first） | 2026-04-23 | 次 feature wave(tag / search / UI)前に概念境界を正本化。Tag = 自由文字列の軽量属性、Color tag = 固定 palette の視覚フォーカス属性、Relation = entry 間の型付きリンクを明確に分離。categorical relation は残し新 Tag と併存、structural relation は木構造専用で本ドラフト不変。判断フレーム(4 問) / UI 命名(タグ/カラー/関連/配置/由来) / 5 軸検索(全文/archetype/Tag/Color/Relation) / additive migration 方針(schema bump なし、既存 categorical を即時変換しない) / next slice A-F を整理。実コード変更なし |
| 127 | `ui-vocabulary-tag-color-relation.md` | W1 Slice A: UI vocabulary 固定（docs-only） | 2026-04-23 | #126 の続編。実際に UI で使う日本語 / 英語 label を 10 語の表として固定(タグ / カラー / 関連 / 分類・意味・時系列・由来 / 配置 / 被参照 / 参照)。Tag chip と Color 色バーと Relation list を視覚的に別レイヤに保つ規約、avoid / banned wording 一覧(categorical を "タグ" と呼ばない / structural を "関連" 単独で書かない 等)、5 軸 filter バーの見せ方、検索構文の prefix 予約(`tag:` / `color:` / `rel:` / `type:`、実装は Slice C で)を整理。実コード変更なし、manual 同期は Tag UI 実装着地後 Slice D で対応 |
| 128 | `../spec/tag-data-model-v1-minimum-scope.md` | W1 Slice B: Tag data model additive minimum-scope draft（docs-only） | 2026-04-23 | #126 / #127 の続編。`entry.tags?: string[]` を additive 追加する最小 schema を固定: 欠落=空配列同義、insertion-order 保持、重複・空文字・改行は reject、R1-R8 normalization ルール(trim / max 64 char / max 32 件 / 制御文字 reject / 大文字小文字区別 / NFC は minimum scope 外)、schema_version bump なしの additive migration、既存 categorical relation と併存。`state.tagFilter` (現 categorical peer lid 単一) を `state.categoricalPeerFilter` に rename する計画を 7.1-7.6 で提示、saved-search の persisted key `tag_filter` は旧 key 読み込み互換を 1-2 release 保持。実コード変更なし、rename は別 slice、Slice C/D/E/F を next-step 整理 |
| 129 | Rename slice | `state.tagFilter` → `state.categoricalPeerFilter` 機械的一括 rename | 2026-04-23 | Slice B §7 計画の実施。in-memory は `categoricalPeerFilter` / `SET_CATEGORICAL_PEER_FILTER { peerLid }` / `SavedSearchSourceFields.categoricalPeerFilter` に刷新。persisted JSON は `categorical_peer_filter` を write、legacy `tag_filter` は read-only fallback で 1-2 release 維持。DOM action-name(`filter-by-tag` / `clear-tag-filter`)は renderer 契約維持のため意図的に不変。5027/5027 tests pass(+2 backward-compat)、bundle rebuild。grep audit 済、live reference 残存なし |
| 130 | `../spec/search-filter-semantics-v1.md` | W1 Slice C: Search / filter semantics draft（docs-only） | 2026-04-23 | Rename slice 後の正本として 5 軸(FullText / Archetype / Tag / Color / CategoricalPeer)の semantics を固定。軸間 AND、Tag は軸内 AND-by-default、Color は軸内 OR、Archetype は軸内 OR、FullText / CategoricalPeer は単一値。prefix 構文 `tag:` / `color:` / `type:` / `rel:` を予約(parser は別 slice G)、quote / OR(`\|`) / negation は部分予約のみ。Saved Search additive: `tag_filter_v2?: string[]`(legacy `tag_filter` との名前衝突回避)+ `color_filter?: ColorTagId[] \| null`。flat fallback 契約は変更せず、filter / selection / reveal の責務分離を明示(PR-ε₁/ε₂ 整合)。次 slice D(Tag filter data-path)→ E(Saved Search schema)→ F(Tag chip UI)→ G(parser)推奨 |
| 131 | `src/adapter/ui/render-continuity.ts` (NEW) | A-1/A-2: 再描画起因バグ根本対策 — scroll / focus continuity | 2026-04-23 | `captureRenderContinuity(root)` + `restoreRenderContinuity(root, snapshot)` を新設。sidebar / center-content / meta 各 region の scrollTop + `data-pkc-field` / `data-pkc-log-id` / `data-pkc-lid` による focus 位置を snapshot → restore。renderer.ts から呼ぶだけで再描画後のスクロール飛び・フォーカス消失を防止 |
| 132 | `src/adapter/platform/folder-prefs.ts` (NEW) | A-4: collapsedFolders localStorage 永続化 | 2026-04-23 | `loadCollapsedFolders(containerId)` / `saveCollapsedFolders(containerId, lids)` を新設。localStorage key `pkc2.folderPrefs`、値は `{ [container_id]: string[] }`。container 切り替え時にページリロードなしで各 container の折り畳み状態を復元 |
| 133 | `src/styles/base.css` (MODIFIED) | D-1: Tag chip CSS 最小整備 | 2026-04-23 | Tag chip 表示 / 入力 / フィルタ系クラス群を追加(~150 行)。同時に未参照 3 クラス(`.pkc-attachment-field` / `.pkc-detached-preview-img` / `.pkc-guardrail-info`)を削除、重複 D-1 ルールを統合して CSS サイズ最適化。CSS budget を 90 KB → 94 KB に更新(`build/check-bundle-size.cjs`) |
| 134 | `src/features/search/filter.ts` (MODIFIED) | W1 Slice D: Tag filter 軸 AND-by-default 実装 | 2026-04-23 | `filterByTags(entries, filter)` 追加(AND-by-default、空 Set = 軸無効、missing/empty tags は非マッチ、case-sensitive `===`)。`applyFilters` を 4th param `tagFilter?` に拡張してバックコンパット維持。17 テスト追加 |
| 135 | `src/features/search/query-parser.ts` (NEW) | `tag:` parser 最小 slice | 2026-04-23 | `parseSearchQuery(raw)` → `{ fullText: string, tags: ReadonlySet<string> }` 新設。lowercase `tag:` prefix のみ認識、値は case-sensitive 保持、bare `tag:` はドロップ、`TAG:` 等は FullText 扱い(§5.6)。`applyFilters` / `entryMatchesQuery` / `renderer.ts highlightMatchesIn` が内部で使用。`state.searchQuery` は raw のまま — reducer は strip しない。17 テスト(query-parser.test.ts) + filter.test.ts 12 テスト追加 |
| 136 | `docs/spec/search-filter-semantics-v1.md` (UPDATED) | W1 Tag wave クローズ docs-only sync | 2026-04-23 | §1.1 as-of 追加、§3 Tag 行を ✅ 実装済みに更新、§3.1 Tag 詳細更新、§5.1 を 実装済み/予約 split に再構成、§5.7 を parser 実装済み記述に更新、§9 を着地済み/残作業 split に書き換え、Status 行を W1 wave クローズとして更新 |
| 137 | `../spec/color-tag-data-model-v1-minimum-scope.md` (NEW) | Color tag data model minimum-scope spec（docs-only） | 2026-04-24 | W1 Tag wave クローズ直後の隣接概念整理。`entry.color_tag?: ColorTagId \| null` を additive 追加する最小 schema を固定: 1 entry に 1 color、固定 palette、ID のみ保存(色値は保存しない / theme 変更に追従)、ID は lowercase ASCII fixed、未知 ID は read で `null` にフォールバックしつつ round-trip で保持、schema_version bump なし。filter 軸は OR semantics、`color:` prefix は既に `search-filter-semantics-v1.md` §5.1 で予約、Saved Search `color_filter?: ColorTagId[] \| null` は additive。Tag / categorical relation と自動変換しない契約。palette 具体 ID は次 slice で fix、それまで Slice 2-4(Saved Search / UI / parser)は着手不可。実コード変更なし |
| 138 | `../spec/pkc-link-unification-v0.md` (NEW) | PKC Link Unification v0 foundation spec（docs-only） | 2026-04-24 | Color UI の前に置く参照基盤正本化。target と presentation を厳密に分離: **target** は `entry:<lid>` / `asset:<key>` / **permalink** `pkc://<container_id>/entry/<lid>[#frag]`、**presentation** は link `[label](entry:...)` / embed `![alt](entry:...)` / card `@[card](entry:...)` の 3 形を記法で区別。paste 変換は permalink → internal への **降格は同一 container のみ**、cross-container は permalink 維持。`@[card]` 採用の根拠(§10.1): target/presentation 混同回避 / 既存 markdown fallback / extract scanner 単一化 / variant 拡張余地。missing target は body から消さず placeholder 描画、循環 embed は 1 段展開、export は subset scanner 経由で依存閉包。`schema_version` bump なし、既存 `entry:` / `asset:` grammar(`entry-ref.ts`)は不変。Slice 1-5(permalink parser → paste → cross-container render → card render → share UI)を next-step として整理、実コード変更なし |
| 139 | `src/features/link/paste-conversion.ts` (NEW) | Paste Conversion Engine — Link system safe entry point（impl slice） | 2026-04-24 | spec #138 §7 の paste 変換規則を最小実装。**Link system の唯一の入口** として、clipboard / DnD / P2P 受信 / extension 入力すべてが将来ここを経由する前提の pure function を先に確定。`convertPastedText(raw, currentContainerId)` → `{ type: 'internal' \| 'external', target, presentation: 'link' }` を返す。`pkc://<container_id>/entry/<lid>[#frag]` を解析し、**自 container_id と exact match した場合のみ** `entry:<lid>[#frag]` に降格(§7.1)、差分があれば permalink 維持(§7.2)、`entry:` / `asset:` は internal pass-through(§7.3)、plain text / https URL / malformed permalink は external fallback(§7.4)。冪等性 / fragment 保存 / 非 string 防御 / cross-container safety を 31 テストで固定。embed / card は別 slice、renderer / markdown parser は一切触らない。Slice 順序を **parser 先行 → paste 先行に修正**(paste が実質の UX 入口で依存方向の根、parser は中間部品)した判断を反映 |
| 140 | `src/features/link/permalink.ts` (NEW) + paste-conversion refactor | Permalink parser / formatter — grammar 単一正本化（impl slice） | 2026-04-24 | `parsePermalink` / `formatPermalink` / `isSamePermalinkContainer` を pure helper として独立。spec #138 §4 / §5 の `pkc://<container_id>/<entry\|asset>/<id>[#fragment]` grammar をこの 1 モジュールに集約。paste-conversion.ts から内部 `parsePkcPermalink` を削除し、新 helper を import して呼ぶ形にリファクタ — **grammar の二重実装を排除**、以降の intake surface / renderer / subset export / share UI はすべて本 helper 1 本を再利用。`ParsedPermalink` は `kind / containerId / targetId / fragment? / raw`、formatter は null-safe(invalid は throw せず null、asset + fragment も null)、parse/format 円環、bare `#` 正規化、case-sensitive 同一 container 判定、空 / 非 string currentContainerId は false 固定(bootstrap 安全側)。37 新テスト + 既存 31 tests 無損傷。renderer / markdown / paste event wiring は対象外 |
| 141 | `src/adapter/ui/link-paste-handler.ts` (NEW) + action-binder wire | Paste event wiring — PKC permalink 貼付時に internal markdown link 化（adapter slice） | 2026-04-24 | spec #138 §6.1 / §7 を editor の paste event で実成立させる adapter slice。`maybeHandleLinkPaste(target, raw, currentContainerId)` を新 helper として追加、pure layer の `convertPastedText` を呼んで same-container 時のみ `[](entry:<lid>)` / `[](asset:<key>)` を textarea/input に splice。`execCommand('insertText')` 優先 → undo stack を保ちつつ既存 `input` event 経路と整合、fallback で setRangeText + synthetic input dispatch。action-binder.ts `handlePaste` の text/plain branch に最小フックを追加(image / HTML link path より前、scope は `body` / `textlog-append-text` / `textlog-entry-text` の既存 allowlist)。internal 確定時のみ `preventDefault`、cross-container / malformed / 普通の URL / plain text / bare `entry:` / `asset:` 直接貼付 / container_id 未設定 はすべて no-op で既定 paste 維持(挙動破壊ゼロ)。15 unit tests + 9 integration tests 追加、bundle.js は wire-in により 654 KB → 656 KB(+2 KB)。renderer / card / embed / share UI / DnD / P2P は対象外、次の自然な slice は Copy permalink UI |
| 142 | `action-binder.ts` + `renderer.ts` + `attachment-presenter.ts` (MODIFIED) | Copy permalink UI — Link system UX loop を閉じる（adapter slice） | 2026-04-24 | spec #138 §4(canonical permalink)の反対方向の導線。Entry meta pane header に `🔗 Copy link` 小ボタン、attachment action row(Download 隣)にも `🔗 Copy link` ボタンを追加。`copy-entry-permalink` / `copy-asset-permalink` action を action-binder に追加し、`formatPermalink({kind, containerId, targetId})` で `pkc://<cid>/entry/<lid>` / `pkc://<cid>/asset/<key>` を生成、既存 `copyPlainText` で clipboard 書込、成功/失敗は `showToast` の既存パターン。`container_id` 未設定時は error toast + clipboard touch なしで safe fail、legacy inline attachment(asset_key なし)は asset 側ボタン自体を非表示。これで **copy → paste → 同一 container なら internal link に自動変換** の UX loop が成立。grammar は permalink.ts 単一正本を再利用(二重実装なし)。8 新 integration tests、bundle.js 656 → 658 KB(+2 KB、budget 648/642 KB = 99.2%)。card / embed renderer / markdown parser 拡張 / share dialog / cross-container renderer / P2P / DnD は対象外 |
| 143 | `markdown-render.ts` + `detail-presenter.ts` + `renderer.ts` + `base.css` (MODIFIED) | Cross-container permalink placeholder — body 内に残った外部 PKC 参照の最小表示（rendering slice） | 2026-04-24 | spec #138 §8.3 を満たす最小表示。markdown-it の `SAFE_URL_RE` に `pkc:` を追加し `link_open` rule を拡張、`[](pkc://<other>/entry/<lid>)` のような cross-container permalink を `parsePermalink` で解析して `class="pkc-permalink-external"` + `data-pkc-permalink-container` / `-kind` / `-target` / `-fragment` をタグ付け。`renderMarkdown(text, { currentContainerId })` に optional 引数を追加、`DetailPresenter.renderBody` に 6 番目引数として container_id を thread、renderer.ts の 2 call site から `container.meta.container_id` を渡す。CSS は `base.css` に `.pkc-permalink-external` badge(🌐 prefix + border + accent 色、~30 行)。同一 container の permalink は通常 anchor(TODO: 将来 internal 降格)、malformed pkc:// は通常 external URL 扱い(body 非表示ゼロ)、現行 `https://` / `entry:` 挙動は不変。元 href は anchor に完全保持、P2P / import / share の将来 resolver が再 parse 不要。renderer-level 10 tests、bundle.js +0.83 KB(643.57/648 KB)/ CSS +0.35 KB(91.42/94 KB)、いずれも budget 内。card / embed / fetch / P2P 解決 / markdown parser 全面拡張は対象外 |
| 144 | spec + features/link/* + adapter/ui/* + base.css + budget (CORRECTION) | Link terminology / grammar correction — `pkc://` ≠ permalink、3 概念分離 | 2026-04-24 | **設計訂正 PR**。初版 #138 spec で `pkc://` を「外部共有 permalink」と扱っていたのは誤り(OS protocol handler 不在のため Loop / Office / mail で開けない)。3 概念に分離: **Internal Reference**(`entry:` / `asset:`、同 container body 用)/ **Portable PKC Reference**(`pkc://<cid>/...`、PKC 内部 / 間 portable identifier、外部クリック不可)/ **External Permalink**(`<base>#pkc?container=<cid>&entry=<lid>`、外部アプリでクリック可能な共有 URL)。spec §3-§5 / §7 / §11(migration)/ §12(URI scheme allowlist) を全面改訂。`features/link/permalink.ts` に `parseExternalPermalink` / `formatExternalPermalink` を新設、`pkc://` 系を `parsePortablePkcReference` 等へ rename(旧名は @deprecated alias で互換維持)。`paste-conversion` を **両形受理**(External Permalink + Portable Reference 両方を same-container 判定で internal 降格)に拡張。`link-paste-handler` は trigger を `pkc://` と `#pkc?` の両方で起動。Copy link UI は `formatExternalPermalink` で `<base>#pkc?...` を emit(`window.location.href` から hash 除去した base URL を使用、file:// / https:// 両対応、外部アプリでクリック可能)。markdown renderer の placeholder class を `pkc-permalink-external` → `pkc-portable-reference-placeholder` にリネーム、data 属性も `data-pkc-permalink-*` → `data-pkc-portable-*` に。URI scheme non-interference を 12 ケース(Office / obsidian / vscode / mailto / tel 等)で網羅テスト。size budget は ユーザー指示で **648 KB → 1.5 MB(1536 KB)** に拡張(現在 644.82 KB、42% 余裕)。tests +58(External Permalink 25 / paste 拡張 26 / link-paste 5 / action-binder integration 2)、合計 5370/5370 pass。card / embed / P2P resolver / OS helper / migration tool は対象外 |
| 145 | `src/adapter/ui/external-permalink-receive.ts` (NEW) + `main.ts` (MODIFIED) | External Permalink receive — 外部 URL クリック → 該当 entry へジャンプ（impl slice） | 2026-04-24 | spec #144 の Copy 側に対する **受信側完成**。`<base>#pkc?container=<cid>&entry=<lid>`(または `&asset=<key>`)で PKC2 を開くと、bootstrap 後に `window.location.href` を `parseExternalPermalink` で読み、同一 container なら `SELECT_ENTRY` を `revealInSidebar: true` で dispatch。これで Loop / Office / mail に貼ったリンクから該当 entry へ戻る UX が成立。挙動: entry permalink → 該当 lid 選択+sidebar reveal、asset permalink → 所有 attachment entry を `body.asset_key` lookup で特定して同様に選択(asset preview modal は作らない、最寄り stable surface としての attachment entry へ着地)、cross-container / missing target / malformed → safe no-op + `info` toast、ordinary `#section` hash や hash なし URL は完全無干渉。bootstrap 3 boot path(pkc-data / idb / empty)に共通 `applyExternalPermalinkOnBoot(dispatcher, container)` を追加。grammar 二重実装なし — `parseExternalPermalink` (#144 で作成)を再利用。21 新 tests、合計 5391/5391 pass。bundle.js +1.18 KB(645.97 KB / 1536 KB = 42.1%、十分な headroom)。card / embed / hashchange listener / P2P / OS protocol handler / 同一ページ内 hash 変更追従は対象外、次は **same-container Portable Reference の internal fallback rendering** が自然 |
| 146 | `markdown-render.ts` + `permalink-placeholder.test.ts` (MODIFIED) | Same-container Portable Reference fallback rendering（spec §5.5 TODO 回収） | 2026-04-24 | body 内に手入力 / import 経由で残った `[](pkc://<self>/entry/<lid>[#<frag>])` を、等価な `entry:<lid>[#<frag>]` として navigate-entry-ref に合流させる最小実装。spec #144 訂正で `pkc://` は paste conversion が通常内部形に降格する設計になったが、writer が手で portable form を書いたり旧 PKC import でそのまま残ったりするケースがあるため、renderer 側に fallback を追加。挙動: `pkc://<self>/entry/<lid>` → anchor に `data-pkc-action="navigate-entry-ref"` + `data-pkc-entry-ref="entry:<lid>[#<frag>]"` を付与(既存 `entry:` リンクと完全同一属性)、既存 `action-binder.ts` の navigate-entry-ref ハンドラが何も変更なしで動作。fragment は verbatim 保持、href も raw 維持(copy / resolver 用)。asset 同 container(`pkc://<self>/asset/<key>`)は navigate-entry-ref 非対応のため今回は対象外(owner attachment resolve は action-binder 側の拡張が必要、follow-up slice)。`currentContainerId` 不明時は安全側(external placeholder)維持。cross-container 挙動は不変。3 新 tests(fallback attrs / fragment / asset は fallback しない)、合計 5394/5394 pass。bundle.js +0.15 KB(646.12 KB / 1536 KB = 42.1%)、CSS 不変。Link system 整合性仕上げ、次は card / embed 本体 or asset owner-resolve 統合 |
| 147 | `link-paste-handler.ts` + `action-binder.ts` (MODIFIED) | fix(link): paste 時の empty label bug 修正 — `[](entry:lid)` → `[Title](entry:lid)` | 2026-04-24 | バグ報告対応: TEXTLOG にパーマリンクを貼付すると `[](entry:mnx73mzy-0004)` のような空 label link が挿入され、CommonMark 仕様(link text 必須)により anchor が不可視表示になる問題を修正。spec §6.1 が renderer 側で title 補完を想定していたが未実装だった。**paste 時に entry title / attachment name から label を合成**する方針で修正(`copy-entry-ref` / `copy-asset-ref` と同じパターン、copy path と一致)。`maybeHandleLinkPaste(target, raw, cid, entries?)` に第 4 引数を追加、`resolveLabel()` helper で `entry:<lid>` → entry.title、`asset:<key>` → attachment.body.name をルックアップして `escapeMarkdownLabel` で `]` / `\` を escape、結果を `[<label>](<target>)` として挿入。entries 未指定 / 空 title / 不在 entry / 孤立 asset は `(untitled)` fallback で anchor を可視に保つ。action-binder.ts の caller は `state.container?.entries` を渡すよう更新。entry rename 後の label stale は許容(既存 copy path と同じトレードオフ)。既存 tests 更新(empty label 期待 → title 期待)+ 5 edge case test 追加(empty title / missing entry / orphan asset / escape / no entries)、合計 5399/5399 pass。bundle.js +0.68 KB(646.79 KB / 1536 KB = 42.1%)。Link 3 層整合は維持、spec §6.1 のコメントは実装側に追従 |
| 148 | `docs/manual/05_日常操作.md` + `09_トラブルシューティングと用語集.md` (MODIFIED) | Link system manual / docs sync（docs-only） | 2026-04-24 | Link system 実装が #138/#141/#142/#143/#144/#145/#146/#147 で完結したことをマニュアル側に反映する docs-only sync。`05_日常操作.md` に新規 `## PKC のリンクをコピー・貼付・開く` 節(約 60 行)を追加、**3 層用語**(External Permalink / Portable PKC Reference / Internal Reference)+ Copy link / Paste conversion / External Permalink receive / Portable Reference fallback の各導線を整理。未実装項目(card / embed / P2P resolver / OS protocol handler / automatic rewrite)を "今はできないこと（future）" として明記。`09_トラブルシューティングと用語集.md` に 3 件のトラブルシューティング追加(`pkc://` を外部に貼っても開けない / External Permalink クリックで対象が開かない / paste しても Markdown リンクに変換されない)+ 用語集に External Permalink / Portable PKC Reference / Internal Reference の 3 エントリを追加(LID の直後、Link 系を集約)。spec は既に #144 訂正で反映済みなので変更なし、manual 更新は Tag wave と同じく「実装着地後」運用パターン。実コード変更なし、card / embed 実装の前に用語と導線を正しく揃えるのが目的 |
| 149 | `docs/development/link-system-audit-2026-04-24.md` (NEW) + `docs/spec/pkc-link-unification-v0.md` (MODIFIED) | Link system audit + 正本化計画（docs-only、Phase 0 棚卸し） | 2026-04-24 | 計画監督モード復帰後の最初の作業。Link system の **Copy / Paste / Render / Receive 4 surface を全棚卸し**、gap list を優先度付きで整理、通常ユーザー向け Copy 導線を External Permalink に 1 本化する正本方針、Internal Markdown Dialect(target / presentation / legacy / 禁止形)、Migration policy(Normalize PKC links tool、preview 必須・opt-in・新 revision 記録)、URI scheme non-interference allowlist を確定。gap 7 件を G1-G7 で列挙: **G1 TEXTLOG log External Permalink copy 未実装 / G2 `copy-log-line-ref` が legacy fragment `#<logId>` を新規 emit / G3 `pkc://<self>/asset/` の renderer fallback 未実装 / G4 Receive `fragment=log/` scroll 未反映 / G5 migration 方針未固定 / G6 Copy surface 2 系統混在 / G7 空 label 経路の残存可能性**。次 PR 順序案(Phase 1 Link 正本化 → Phase 2 Migration → Phase 3 Version/Changelog → Phase 4 card/embed)を 8 段階で提示。spec §1 冒頭に audit pointer、§5.7 Internal Markdown Dialect の正本表、§11.6 Normalize PKC links tool 方針を追記。実コード変更なし、実装 PR(TEXTLOG log copy / asset fallback / migration tool)は次 wave 以降 |
| 150 | `action-binder.ts` + `renderer.ts` + `textlog-presenter.ts` (MODIFIED) | Phase 1 step 2: TEXTLOG log External Permalink copy（G1 + G2 解消） | 2026-04-24 | `copy-log-line-ref` action の出力を **External Permalink** に切替。audit §3.3 G1(TEXTLOG log の External Permalink copy 未実装)と G2(legacy fragment `#<logId>` 新規 emit)を同時解消。action 名は `copy-log-line-ref` のまま維持(DOM binding 安定性のため、audit §8.2 推奨)。挙動: `[title › ts](entry:<lid>#<logId>)` legacy internal form → `<base>#pkc?container=<cid>&entry=<lid>&fragment=log/<logId>` **canonical External Permalink**(fragment 値は URL-encoded、`log/` プレフィックス必須、spec §5.1 canonical)。grammar 二重実装なし — 既存 `formatExternalPermalink` + `currentDocumentBaseUrl` を再利用。UI: context menu は `📝 Log ref` → `🔗 Copy link` / tip も更新、row inline `🔗` ボタン tooltip を `このログ行の共有リンクをコピー` に。container_id 未設定時は error toast + clipboard 無変更(safe fail)。`formatLogLineReference` ヘルパは唯一の caller が消えたため削除(dead code 回避)。挙動テスト: 既存 1 件(legacy internal 期待 → External 期待)に更新 + 3 新規(canonical fragment form 確認 / inline button と context menu の parity / container_id 未設定の safe fail)、合計 5402/5402 pass。bundle.js +0.35 KB(647.13 KB / 1536 KB = 42.1% OK)。paste conversion 側の log-label support 拡張は次 PR、renderer / parser の legacy internal form 互換読み取りは維持 |
| 151 | `link-paste-handler.ts` + test updates (MODIFIED) | Phase 1 step 3: Paste conversion log-label support（#150 と対) | 2026-04-24 | 直前 #150 で External Permalink 化した TEXTLOG log link を **PKC 内 editor に貼り戻したときの label 合成** を実装。空 label 禁止ルール(spec §5.7.4)に沿って、`entry:<lid>#log/<logId>` target に対して `<entry title> › <log snippet>` の形で動的に label を合成。`link-paste-handler.ts` の `resolveLabel` に log fragment 専用分岐を追加、`parseTextlogBody` で row を lookup して `resolveLogSnippet` ヘルパで snippet を生成(1 行化 + whitespace collapse、40 char 超は `…` で truncate、空 text は `createdAt` ISO timestamp にフォールバック)。log row missing / archetype mismatch(TEXT entry + `#log/...`)は `<title> › Log` または entry title に generic fallback、target は保持して後からの復活を妨げない。range fragment(`log/a..b`)/ heading fragment(`log/<id>/<slug>`)は小粒度の単一 row ではないため generic fallback、legacy bare `<logId>` fragment(`log/` プレフィックスなし)は **auto-normalise しない**(paste 側で無理に canonical に寄せない、migration tool の仕事)。Copy(#150) → Paste(本 PR)で TEXTLOG log の UX loop が閉じる。tests: 9 unit tests 新規(snippet / truncate / timestamp fallback / missing / non-textlog / legacy bare / range / heading / portable `pkc://` パス)+ 1 integration test(action-binder 経由 end-to-end)、合計 5412/5412 pass。bundle.js +0.47 KB(647.59/1536 KB = 42.2% OK)、CSS 不変。cross-container / ordinary URL / Office URI / 既存 entry / asset / fragment なし path の regression 0 |
| 152 | `markdown-render.ts` + `action-binder.ts` + 新規 test file | Phase 1 step 4: `pkc://<self>/asset/<key>` internal fallback rendering（audit G3 解消） | 2026-04-24 | body 内に残った same-container Portable asset reference `[label](pkc://<self>/asset/<key>)` を **クリックで所有 attachment entry へ遷移** させる最小実装。audit §7 G3 の最後のピース。`markdown-render.ts` の pkc:// 同 container 分岐を拡張、`kind === 'asset'` の場合に `data-pkc-action="navigate-asset-ref"` + `data-pkc-asset-key="<key>"` を付与(entry branch と同じ fallback パターン)。`action-binder.ts` に `navigate-asset-ref` handler を新設、`findAttachmentOwnerLid(dispatcher, assetKey)` helper で `entry.archetype === 'attachment'` かつ `body.asset_key === <key>` の entry を捜索、見つかれば `SELECT_ENTRY + revealInSidebar: true` を dispatch(External Permalink receive の asset 挙動と同じ着地)、missing は info toast で safe no-op。malformed attachment body(非 JSON)は skip。`e.preventDefault()` を必ず呼ぶ(`pkc://` は OS で解決不能のため native navigation を止める)。cross-container(`pkc://<other>/asset/<key>`)は **既存 portable-reference-placeholder のまま維持**(G3 のスコープは same-container のみ)、malformed URI や ordinary URL / Office URI scheme は横取りしない。テスト: permalink-placeholder.test.ts の asset case を新挙動に更新(既存 `data-pkc-action` 無期待 → `navigate-asset-ref` 期待) + cross-container 不変確認 + malformed fallback の 2 件を追加、新規 integration test ファイル `action-binder-navigate-asset-ref.test.ts`(5 tests)で click → SELECT_ENTRY / preventDefault / action 名正確性 / missing owner safe no-op / malformed body skip を網羅。合計 5419/5419 pass(+7)。bundle.js +0.72 KB(648.29/1536 KB = 42.2% OK)、CSS 不変。Phase 1 の Link 正本化は Step 4 までで基本完了、残り Step 5(context menu Internal copy 整理、G6)のみ |
| 153 | `renderer.ts` + test updates (MODIFIED) | Phase 1 step 5: context menu Internal Reference copy 整理 (audit G6 解消) | 2026-04-24 | 通常ユーザー向けリンク取得導線を **`🔗 Copy link` = External Permalink** に 1 本化するための最小 UX cleanup。機能追加なし、UI terminology cleanup のみ。`renderContextMenu` 内の legacy Internal Reference copy 3 件(`copy-entry-ref` / `copy-asset-ref` / `copy-entry-embed-ref`)を **`📝 Markdown source` group に集約**、`📝 Markdown link` / `📝 Markdown asset link` / `📝 Markdown embed` へ rename。context menu 描画で `group === 'markdown-source'` の最初の項目の直前に separator + `📝 Markdown source` 見出しを emit(1 回のみ、追加 DOM は 2 要素のみ)、項目に `data-pkc-menu-group="markdown-source"` を付与。これで通常ユーザーは `🔗 Copy link`(TEXTLOG log context menu、Entry meta pane、Attachment card)が外部共有用 External Permalink、`📝 Markdown source` 配下がパワーユーザー向け Markdown 内部参照という区別が即座につく。action 名は全て維持(DOM binding 不変)、出力形式は全て不変(legacy 互換完全維持)。既存 renderer.test.ts の 2 件(`📎 Asset ref` 期待 / `Entry ref` + `Embed ref` 期待)を新 label に更新、1 件強化(grouping attrs + section header の存在確認)。合計 5419/5419 pass、bundle.js +0.42 KB(648.70/1536 KB = 42.2% OK)、CSS 不変。これで **Phase 1(Link 正本化の実装修正)完了**、次は Phase 2(Link migration tool docs-first)|
| 154 | `docs/spec/link-migration-tool-v1.md` (NEW) | Phase 2 step 1: Normalize PKC links migration tool docs-first（docs-only） | 2026-04-24 | Phase 1(新規 emit の正本化)完了後に Phase 2 の土台として、既存 body に残った legacy PKC link forms を **preview + user-approved apply** で正本 Internal Markdown Dialect(spec §5.7)に寄せる migration tool の契約を 691 行 / 13 section で固定。docs-only、実装ゼロ。変換候補 4 分類: **A** 空 label link(`[](entry:...)` → `[<Entry Title>](entry:...)`、label 合成は #147 paste-handler 経路を再利用、safe)/ **B** legacy TEXTLOG fragment(`#<logId>` → `#log/<logId>`、row id 一致時のみ safe-with-match)/ **C** same-container Portable Reference(`pkc://<self>/...` → `entry:` / `asset:`、safe)/ **D** legacy asset image embed(`![](asset:...)` → `[![]](asset:...)`、default OFF の option 扱い、review)。非対象: ordinary URL / Office URI / obsidian / vscode / 未知 scheme(絶対不干渉、§4.1 の 12 scheme allowlist)/ cross-container pkc://<other>/ / non-empty user label 上書き / unknown fragment / code block・inline code 内 / raw HTML。Scanner 設計: markdown full AST 不要の conservative regex + code-fence masking、features 層 pure function、既存 `parseEntryRef` / `parsePortablePkcReference` / `parseExternalPermalink` を再利用(grammar 二重実装なし)、archetype 別 scope 表(text / textlog row 個別 / folder / todo / form、attachment metadata と system entry は対象外)。Preview model: `LinkMigrationCandidate`(entryLid / archetype / location = body \| textlog{logId} / kind / before / after / confidence / reason)+ determinism 保証。UI flow: Tools → Normalize PKC links → preview dialog → Apply all safe / Apply selected / Cancel、editing phase / readonly / light mode / import preview は Apply block。Apply semantics: entry 単位 atomic、新 revision に `operation: 'link-migration'` を記録して undo 可能、pre-apply re-scan で data-race guard、system-reserved entry は常に skip。Safety levels: A/C は default ON、B は row-id match 時のみ safe、D は default OFF option。実装 slices: 1) pure scanner + preview model、2) preview UI dialog、3) apply reducer + revision 連携、4) manual 更新、の 4 段階 + optional Slice 5(web worker 化)。本 spec が固まることで Phase 3 Version/Changelog/About v2.1.0 に "Link migration tool は設計済み・未実装、Slice 1-4 で順次実装予定" と Known limitations を正確に書ける |
| 155 | `package.json` + `release-meta.ts` + `about-payload.ts` + `about-entry-builder.ts` + `renderer.ts` + 2 docs (MODIFIED/NEW) | Phase 3: Version / Changelog / About v2.1.0 可視性修正 | 2026-04-24 | v2.0.0 のまま多数のユーザー可視機能(Link system / Tag wave / Storage Profile / UI continuity / data correctness)が積み上がっていた状態を是正し、**user-visible version を v2.1.0 に引き上げ**。semver source of truth 3 箇所(`package.json` / `src/runtime/release-meta.ts` VERSION / `build/about-entry-builder.ts` RELEASE_SUMMARY)を同時更新。AboutPayload に `release?: { highlights: string[]; knownLimitations: string[]; changelog?: string }` を **additive** 追加(`schema_version` は bump しない、optional フィールドなので旧 export も validation 通過、validator 側に optional narrowing + `filterValidRelease` helper)。`renderAboutView` に `renderAboutRelease` helper を新設、About セクション末尾 credits の直前に highlights / knownLimitations リスト + changelog pointer を描画(`data-pkc-region="about-release"` / `about-release-highlights` / `about-release-limitations`)、旧 export / dev build で release block 欠落なら section ごと省略。新規 docs: **`docs/release/CHANGELOG_v2.1.0.md`**(v2.1.0 release note、Highlights / Link system 3 層用語 / Tag / Storage / UI continuity / Data correctness / Relation safety / UX polish / Known limitations / Migration 注意 / 参照 pointer)+ **`docs/development/versioning-policy.md`**(major/minor/patch 基準、必須同期 3 点 = source of truth / changelog / About / dist rebuild、build integrity ≠ user-visible version、単一 HTML 配布での About 重要性、Link system 3 層用語遵守ルール)。Link system 用語厳守: `pkc://` を permalink と呼ばない、External / Portable / Internal の 3 層を正しく使い分け。Known limitations は正直に列挙(Link migration tool 未実装 / card / embed 未実装 / Color tag spec-only / cross-container resolver 未実装 / OS protocol handler 未実装 / full footprint 未実装)。これで単一 HTML を開いた user が **About 画面だけで「今どの状態か」「何が未実装か」を判断できる**。schema 互換完全維持、container / export 破壊ゼロ |
| 156 | `src/features/link/migration-scanner.ts` (NEW) + tests | Phase 2 Slice 1: Normalize PKC links pure scanner + preview model | 2026-04-24 | Phase 2 の実装フェーズ最初のピース。spec `docs/spec/link-migration-tool-v1.md` §5(scanner)/ §6(preview model)/ §10(tests)を features 層 pure function として実装。`buildLinkMigrationPreview(container, options?) → { candidates, summary }` と convenience `scanLinkMigrationCandidates(container, options?)` を export。Candidate 3 種(A 空 label / B legacy log fragment row-id match 時のみ / C same-container Portable Reference)を分類、label 合成は `link-paste-handler` の `resolveLabel` と同じ規約を pure に再実装(entry title、log snippet 40 char truncate + `…` + timestamp fallback、attachment name、`(untitled)` 最終 fallback)、escape は inline helper。grammar は既存 `parseEntryRef` / `parsePortablePkcReference` / `parseTextlogBody` を再利用(二重実装なし)。非干渉: URL scheme allowlist(12 種 + 未知 scheme)/ cross-container `pkc://<other>/...` / non-empty canonical / unknown fragment / image-form on entry/portable / malformed body / 全てテストで固定。Code block 非干渉は `maskCodeRegions` で fenced(``` / ~~~)+ inline code を blank 置換、オフセット保持でマッチ位置は元テキスト基準。archetype scope: text / textlog(row 単位で `parseTextlogBody` → `row.text` 個別 scan)/ folder / todo / form、attachment + system-reserved(`__*__` / `system-*`)は skip。Deterministic order: container.entries → entry 内 offset 昇順 → textlog row 配列順 → row 内 offset 昇順(テストで pin)。Malformed body(JSON 破損 / 不均衡 brackets)は throw せず silent skip。**#157 follow-up で Candidate D を撤去、canonical image embed の regression tests を追加**(最終 test 数は #157 行参照)。DOM / state / I/O 非依存、bundle.js は未 wire-in のため tree-shake で不変。次 slice(UI dialog / apply reducer)は本 scanner を consumer として利用 |
| 157 | `src/features/link/migration-scanner.ts` + tests + `docs/spec/link-migration-tool-v1.md` + `docs/spec/pkc-link-unification-v0.md` (MODIFIED) | Phase 2 Slice 1 follow-up: Harbor Philosophy で Candidate D を撤去、future dialect reservations を明文化 | 2026-04-24 | PR #126 内の follow-up。2026-04-24 に行った Markdown standard compatibility + PKC dialect design audit の結論を適用:scanner v1 から **Candidate D(legacy-asset-image-embed)** を削除、`LinkMigrationCandidateKind` union / `ScanOptions.enableAssetEmbedMigration` option / `classifyAssetHref` 内の D branch / summary の byKind / module doc の "Four candidate kinds" を全て A-C の 3 kinds に縮約。理由 (Harbor 原則): (a) `![<alt>](asset:<key>)` は asset resolver が `data:` URI に展開する **現行 canonical image embed** であり触る理由がない、(b) 旧 spec §3.5 が示していた after 形 `[![<alt>]](asset:<key>)` は markdown-it が literal `![]` label の anchor として token 化し clickable image にならない、(c) 意図を汲んだ nested 形 `[![<alt>](asset:<key>)](asset:<key>)` は標準 CommonMark の clickable-image だが現行 `SAFE_URL_RE` に `asset:` が無いため outer link が reject され literal 漏れする(2026-04-24 `markdown-it` + `renderMarkdown` probe で実測)。scanner v1 で apply すれば 4 層(入港 / 定泊 / 出港 / 座礁回避)のうち "座礁回避" を破るため、future dialect として reservation に降ろす。**regression tests 5 件を新設**:(1) `![alt](asset:<key>)` は candidate 化しない、(2) `![](asset:<key>)` も candidate 化しない、(3) `[![alt](asset:<key>)](asset:<key>)` は v1 では扱わない、(4) `[![]](asset:<key>)` は扱わない、(5) `[![]](entry:<lid>)` も扱わない。Candidate D の 2 件テストは削除。A/B/C のテストと non-interference / code block masking / archetype scope / deterministic order / summary / malformed / facade の既存 pin は完全維持。spec 更新: `docs/spec/link-migration-tool-v1.md` §3.5 を「Candidate D」から「Non-candidate — Already canonical」に改題し canonical 保護を強化、§3.6 / §3.7 整理、§6.1 union 型を 3 kinds に、§9.1 safety level table から D 行を削除し canonical / future dialect の "touch しない" 行を新設、§2.2 含めない欄に "future dialect 生成" を追加、§14 Future dialect reservations 節を新設(14.1 clickable-image / 14.2 bracket-wrapped empty label / 14.3 `@[card]` / 14.4 共通原則)、旧版 Candidate D の経緯と削除理由を §3.5 末尾に注記。`pkc-link-unification-v0.md` §5.7.4 禁止リストに `[![<alt>]](<target>)` を追加、§5.7.5 Future dialect reservations 小節を新設(3 form × 標準 Markdown扱い / 現行 PKC2 扱い / 将来の検討 の 3 列で整理、harbor 原則 cross-reference)。Bundle は未配線のため size 不変、scanner consumer(UI dialog)は次 slice で接続 |

| 158 | `docs/development/clickable-image-renderer-audit.md` (NEW) + `docs/spec/link-migration-tool-v1.md` + `docs/spec/pkc-link-unification-v0.md` (MODIFIED) | Clickable-image renderer audit(docs-only、future dialect 航路図固定) | 2026-04-24 | PR #126 follow-up #157 で Candidate D を scanner v1 から撤去した後の、**標準 Markdown の clickable-image `[![alt](url)](url)` を PKC が将来どう受け入れるか** を docs-only で固定する設計 audit。12 section / 11 syntax × 9 列の audit matrix / 8 decisions / future-v2 実装 scope / migration v1/v2 境界を明文化、src/ / migration scanner に一切触れない。Harbor Philosophy(入港 / 定泊 / 出港 / 座礁回避)を評価軸に再掲し、syntax 11 form(`![alt](asset:key)` 〜 External Permalink link form まで)を 9 列で評価。現在地整理: `SAFE_URL_RE` が `asset:` を含まない事実、asset-resolver の 2-pass 規則、`navigate-entry-ref` / `navigate-asset-ref` / `#asset-` chip 経路、image rule の transclusion placeholder、paste-conversion / external-permalink-receive の責務を code 引用で pin。Presentation taxonomy: canonical now(既存)/ accepted legacy(scanner v1 対象)/ future dialect(renderer 整備後、clickable-image image / clickable-transclusion entry / `@[card]` / body 内 External Permalink 救済)/ invalid(`[![]](target)` / `[card:<lid>]` / body 内 pkc:// 新規 emit)。Decisions to pin(8 点): (1) `asset:` を `SAFE_URL_RE` に追加する前提は resolver 拡張後、(2) SAFE_URL_RE 追加時は action-binder に `a[href^="asset:"]` handler を同時追加、(3) clickable-image 正式サポートには markdown-render / asset-resolver(pass 0 新設、後方参照で inner/outer key 一致のみ)/ action-binder(既存 `navigate-asset-ref` data 属性流用)+ tests + spec 更新 + migration v2 scanner が必要、(4) `![alt](asset:k)` canonical と clickable-image は責務別で共存可(画像だけ vs 画像 + click)、(5) clickable-transclusion は block-in-inline HTML semantic 問題を renderer が解決してから、(6) `[![]](target)` は明示的に invalid / do-not-emit、(7) `@[card]` と clickable-image は用途別・別 hook で共存、(8) migration v1 は legacy → 現行 canonical のみ、v2 は future dialect への opt-in promotion。Migration v1/v2 境界サマリ表: scanner module / candidate kinds / safety default / renderer 前提 / harbor validation / spec 担い / UI dialog / revision operation tag を 2 列で並べ、API interface は共通化方向に揃える推奨。External Permalink 責務整理: body 内 External Permalink は migration v2 候補(G)として別枠、clickable-image 実装と独立。Next-step PR options: Option A(Phase 2 Slice 2 Preview UI dialog、scanner v1 A/B/C 列挙、~500 LOC src + ~400 LOC tests、adapter additive、リスク低)/ Option B(clickable-image renderer 実装、`SAFE_URL_RE` + resolver pass 0 + link_open 分岐 + tests + spec、~200 LOC src + ~400 LOC tests、入港 / 出港 / 座礁の 3 層改善)、推奨は A を先(既存 canonical regression リスク ゼロ、user feedback が早く取れる)、ただし外部 paste 経路重視なら B が先でも可。spec cross-reference: `link-migration-tool-v1.md` §14.5 / `pkc-link-unification-v0.md` §5.7.5 末尾に本 audit への link を surgical 追加。実装ゼロ、tests 追加ゼロ、dist rebuild なし、build / full test は不要、migration-scanner.test.ts スポット確認で scanner v1 A/B/C が無影響であることを確認 |

| 159 | `src/adapter/ui/link-migration-dialog.ts` (NEW) + `src/adapter/ui/renderer.ts` + `src/adapter/ui/action-binder.ts` + `src/adapter/state/app-state.ts` + `src/core/action/user-action.ts` + `src/main.ts` (MODIFIED) + `tests/adapter/link-migration-dialog.test.ts` (NEW) | Phase 2 Slice 2: Normalize PKC links preview UI dialog | 2026-04-24 | Phase 2 Slice 2 実装。PR #126 で固めた scanner v1(A 空 label / B legacy log fragment / C same-container Portable Reference)を **ユーザーが preview できる UI dialog** を追加。Apply は Slice 3 に持ち越し、本 slice では disabled button と "Slice 3" ラベルで明示。state-synced pattern(text-to-textlog-modal と同型): identity は `AppState.linkMigrationDialogOpen?: boolean`(runtime-only、optional)、DOM は `syncLinkMigrationDialogFromState(state, root)` で render から reconcile。`OPEN_LINK_MIGRATION_DIALOG` / `CLOSE_LINK_MIGRATION_DIALOG` UserAction を追加、reducer は flag を flip + shell menu も同時に閉じる。Shell menu に新規 `Tools` セクションを追加、`🔧 Normalize PKC links` ボタンが `data-pkc-action="open-link-migration-dialog"` を発行(container 無しなら `disabled` 属性 + `data-pkc-disabled-reason="no-container"`)。action-binder: OPEN は `container` 有り + `phase !== 'editing'` の guard 経由で dispatch、CLOSE はそのまま dispatch、Esc handler は topmost overlay 優先順(TEXTLOG preview → TEXT→TEXTLOG modal → **link migration dialog**(新規) → textlog selection → asset picker)に組み込み。readonly / lightSource / viewOnlySource は preview を塞がない(スキャンは純粋 read-only、Apply は Slice 3 で guard)。dialog 内部:(a) ヘッダ(summary `N candidates across M entries (K safe).`)、(b) body(候補を `<ol>` で entry ごとに group、各 row に `[SAFE/REVIEW]` バッジ + kind ラベル + entry title + archetype + textlog row の `log/<logId>` + before / after `<code>` diff + reason)、(c) footer(`Apply (Slice 3)` 無効化ボタン + "Apply is not available in this slice" note + Close ボタン)、(d) 候補 0 件時は empty state "All PKC links in this container are already in canonical form."。main.ts が boot 時に `setLinkMigrationDialogDispatcher(dispatcher)` を呼び、backdrop click / defensive "no container" close path から `CLOSE_LINK_MIGRATION_DIALOG` を dispatch。kind ラベル:`empty-label → Empty label`、`legacy-log-fragment → Legacy log fragment`、`same-container-portable-reference → Portable PKC Reference`(Candidate D kind は存在しないので表示経路なし)。before / after は `<code>` 要素に `textContent` で書き込み、markdown 記号は HTML escape されず literal で出る(diff 可読性優先、XSS 混入は render 経路で弾く設計)。**16 integration tests** を新設:(1) OPEN → overlay mount、(2) CLOSE → unmount、(3) Close button click → CLOSE dispatch、(4) Esc → CLOSE dispatch、(5) backdrop mousedown → CLOSE、(6) no container → OPEN は block + dialog 非表示、(7) A/B/C 3 種全て列挙 + kind ラベル、(8) before/after diff が literal text(HTML 再 parse しない)、(9) markdown metachar escape(`<script>` が literal text で残る)、(10) scanner deterministic order が UI に反映、(11) Candidate D(`legacy-asset-image-embed`)は表示されない regression、(12) fenced code block 内 link は表示されない(scanner 側既存 pin の延長)、(13) empty state、(14) Apply ボタン disabled + "Slice 3" label + note、(15) readonly でも preview 可 + Apply 無効、(16) 直接 API `openLinkMigrationDialog` / `closeLinkMigrationDialog`。合計 5485/5485 pass(+19: migration-scanner 無変更 50 件 + dialog 新規 16 件 + core/action-types 新規 action による 3 件追加)。Bundle 665.92 KB → **678.21 KB**(+12.29 KB、42.3% → 44.2% of 1536 KB budget)、CSS 93.65 KB 変化なし。dialog は既存 canonical に一切触れず、renderer / asset-resolver / SAFE_URL_RE / clickable-image には未着手(Phase 2 Slice 3 Apply reducer + revision 連携、または Phase 2 B clickable-image renderer 実装がこの後の候補) |

| 160 | `src/features/link/migration-apply.ts` (NEW) + `src/adapter/state/app-state.ts` + `src/core/action/user-action.ts` + `src/core/action/domain-event.ts` + `src/adapter/ui/link-migration-dialog.ts` + `src/adapter/ui/action-binder.ts` (MODIFIED) + `tests/features/link/migration-apply.test.ts` (NEW) + `tests/adapter/link-migration-dialog.test.ts` (MODIFIED) | Phase 2 Slice 3: Link migration apply reducer + revision 連携 | 2026-04-24 | Phase 2 Slice 3 実装。Slice 1 の scanner と Slice 2 の preview UI をつなぎ、**ユーザーが Apply all safe をクリックすると body が実際に書き換わる** 段階まで到達。pure planner(`applyLinkMigrations(container, candidates, now, generateRevisionId, bulkId) → { container, applied, skipped, entriesAffected }`)を features 層に新設し、reducer の `APPLY_LINK_MIGRATION` は guard + 再 scan + planner 呼び出し + state 更新を担う責務分離。layer 検査: reducer は adapter 層で既に features を広範に import しているため、`buildLinkMigrationPreview` / `applyLinkMigrations` を直接呼ぶのは規約内(core ← features ← adapter、reducer は adapter に所属)。**apply algorithm**:(1) entry 単位に group、(2) textlog archetype は `parseTextlogBody` → row.text 書き換え → `serializeTextlogBody`(row id / createdAt / flags は preserve)、plain archetype は body 文字列を直接書き換え、(3) 同一 body / 同一 row 内の複数 candidate は **offset 降順** に置換(earlier rewrite が later offset を shift するのを防止)、(4) 各 candidate の `before` を current body / row.text の `slice(start, end)` と照合、不一致は **skipped** カウントに計上し隣接 candidate は継続、(5) 変更が発生した entry のみ `snapshotEntry` → `updateEntry` の順で適用(既存 QUICK_UPDATE_ENTRY と同型)。**stale guard**: apply 時に preview を **再 scan** して `confidence === 'safe'` のみに絞り、preview 後の edit / dual-edit / quick update で drift した candidate は自然に drop、planner 層の `before` 一致チェックで二重に保護。**guard(destructive action policy)**: reducer + action-binder で readonly / importPreview / editing / lightSource / viewOnlySource / no-container を belt-and-braces block(light-source / view-only-source は persistence 不能なので undo を期待させないため block、preview は readonly でも開ける)。**revision 記録**: 各影響 entry に 1 件の `Revision` を記録、**全件共通の `bulk_id`**(`generateLid()`)を持つため将来の bulk undo UI で group restore 可能、`snapshot` は pre-apply の Entry JSON 全体、`prev_rid` は H-6 契約どおり既存 revision にチェーン、`content_hash` は `snapshotEntry` が FNV-1a-64 で自動付与、`title` / `lid` / `archetype` / `created_at` は不変で `body` + `updated_at` のみ変わる。**DomainEvent** に `LINK_MIGRATION_APPLIED { applied, skipped, entriesAffected }` を追加(subscriber 層の analytics / toast 用)。**AppState** に runtime-only `linkMigrationLastApplyResult?: { applied, skipped, entriesAffected, at }` を additive 追加、`OPEN_LINK_MIGRATION_DIALOG` / `CLOSE_LINK_MIGRATION_DIALOG` で reset。**Dialog 更新**: `Apply all safe` ボタンが candidates 有りで enabled、無効化理由を `data-pkc-link-migration-apply-disabled-reason`(`readonly` / `import-preview` / `light-source` / `view-only-source` / `editing` / `no-candidates`)で明示、apply 後は `data-pkc-link-migration-banner="applied"` + applied / skipped / entriesAffected 属性で結果 banner を render、skipped があれば "Skipped N candidates because the source text changed between preview and apply." の追加行、container reference が変わるので `rerenderBody` が走り残余 candidates が 0 件なら自動で empty state が表示される。`sync` は `refreshFooter` / `refreshBanner` を毎 tick 呼ぶので readonly / importPreview の切り替えも即時反映。action-binder: `apply-link-migration` handler が OPEN と同じ 5 種 guard を経て dispatch。**追加テスト合計 22 件**:(a) `tests/features/link/migration-apply.test.ts` 新規 **16 件**(Candidate A empty label → entry title 合成 / title 不変 / Candidate A asset name / Candidate B legacy log fragment canonicalise / Candidate C entry portable → internal / Candidate C asset portable → internal / textlog row candidate / multi-candidate offset-desc / stale by drift / stale + fresh mixed / textlog 行 id 消失 stale / revision bulk_id 共有 / revision prev_rid チェーン / candidate 空で revision 無し / 入力 container 非 mutation / title/lid/archetype/created_at invariants / empty body + 空 candidate の reference identity)、(b) `tests/adapter/link-migration-dialog.test.ts` 拡張 **+6 件**(Apply ボタン enabled / disabled by no-candidates / disabled by readonly / click dispatch → container rewrite / apply 後 banner + empty state / open 再 open で banner reset / bulk revision per entry で title/archetype 不変)。合計 **5506/5506 pass**(+21 from 5485)、scanner / preview / 既存 pin は全て通過。Bundle 678.21 KB → **684.16 KB**(+5.95 KB、44.5% of 1536 KB budget)、CSS 93.65 KB 変化なし。renderer / asset-resolver / SAFE_URL_RE / clickable-image は未着手、migration v1 は A/B/C の apply までで完結(v2 目標は clickable-image / `@[card]` / body 内 External Permalink などの future dialect、renderer 整備 PR が先行) |

| 161 | `docs/manual/05_日常操作.md` + `docs/manual/09_トラブルシューティングと用語集.md` + `docs/manual/00_index.md` (MODIFIED) + `PKC2-Extensions/pkc2-manual.html` (REBUILT) | Phase 2 Slice 4: Link migration tool v1 を manual / troubleshooting / glossary に同期 | 2026-04-24 | Phase 2 Link migration tool v1 が Slice 1 → 3 で完成したので、ユーザー向けの説明を manual に反映する docs-only slice。src / scanner / dialog / reducer には一切触らない。`05_日常操作.md` の「今はできないこと（future）」節から「旧本文の自動書き換え」を削除し、代わりに Future 候補に clickable-image を追加(v1 対象外・future dialect 予約)、直後に新節 **§PKC リンクを正規化する — Normalize PKC links** を追加(〜80 行)。新節は対象 3 種(空ラベル link / 旧 TEXTLOG fragment / 同一 container Portable PKC Reference)の Before/After 表、非対象リスト(cross-container / ordinary URL / Office URI / clickable-image / canonical image embed / transclusion / code block / raw HTML 等)、preview UI の見た目(`[SAFE]` バッジ / kind label / Before/After コードブロック / Reason)、Apply の動作(再 scan → stale 検出 → offset 降順置換 → revision 記録 + bulk_id → banner "Applied N across M entries" / "Skipped K because source changed")、Apply 無効化条件(readonly / import-preview / light / view-only / editing / no-candidates)、Undo(revision restore)を網羅。`09_トラブルシューティングと用語集.md` にトラブル 4 件追加:(1) Normalize PKC links に候補が出ない(既に正本 / 対象 3 種のみ / code block 除外 / cross-container 対象外 / ordinary URL・Office URI 対象外 / clickable-image v1 対象外 / canonical image embed 対象外)、(2) メニュー項目が灰色(no-container)、(3) Apply all safe が押せない(reason 別 5 種の対処)、(4) skipped された候補が出た(stale guard の説明 + 再 scan で継続 / TEXTLOG 行削除時の扱い)、(5) clickable-image が正規化されない(v1 対象外 + future dialect 予約 + 現行 canonical `![alt](asset:k)` 保持 + 外部 reader 出港互換性)。用語集 3 件追加:**Normalize PKC links** / **Safe candidate** / **Stale candidate**、既存の External Permalink / Portable PKC Reference / Internal Reference の直後に配置して Link system 用語を一箇所で見られるように整理。`00_index.md` の「最近の UX 改善」に bullet 1 件追加:**PKC リンクの一括正規化**(Normalize PKC links への cross-link + preview + Apply all safe + undo を 1 行で紹介)。`npm run build:manual` 成功、`PKC2-Extensions/pkc2-manual.html` 1801.8 KB / 30 entries / 17 relations / 7 assets / full readonly mode に rebuild(sample container 内にも新章反映)。src / tests / bundle には一切変更なし、typecheck / lint / test / build:bundle は docs-only のため実行せず。migration scanner / planner / reducer / dialog は完全に不変、既存テスト 5506 件は影響を受けない。Link migration tool v1 は実装 + docs で閉じた状態、次は Phase 3 の v2.1.1 bump(CHANGELOG + About highlights) / per-candidate selection(Slice 3.5) / clickable-image renderer(別航路)のいずれか |

| 162 | `package.json` + `src/runtime/release-meta.ts` + `build/about-entry-builder.ts` (MODIFIED) + `docs/release/CHANGELOG_v2.1.1.md` (NEW) + `dist/pkc2.html` + `PKC2-Extensions/pkc2-manual.html` (REBUILT) | Phase 3: v2.1.1 bump — Link migration tool v1 完成版 release | 2026-04-24 | Phase 2 Link migration tool v1(Slice 1-4)完成を受けて user-visible version を **v2.1.0 → v2.1.1** に bump。docs/development/versioning-policy.md §3(About summary と changelog が同期する規約)に従って semver source of truth 3 点を同時更新:(a) `package.json` の `"version": "2.1.1"`、(b) `src/runtime/release-meta.ts` の `VERSION = '2.1.1'`(コメントに Phase 2 close の経緯を追記)、(c) `build/about-entry-builder.ts` の `RELEASE_SUMMARY` に `'2.1.1'` ブロックを additive 追加(v2.1.0 ブロックは保持、古い export も正しく About を render できるよう後方互換を維持)。`schema_version` は **1 のまま変更なし**(container shape 不変、v2.1.0 export を v2.1.1 で開ける / v2.1.1 export を v2.1.0 で開ける双方向互換)。**v2.1.1 About highlights(5 件)**: (1) Link migration tool v1 preview + Apply all safe、(2) Revision-backed normalization(bulk_id grouped, restore-capable)、(3) Stale-candidate safety(skip instead of overwrite)、(4) Harbor-safe dialect gate(Candidate D removed + clickable-image future reservation)、(5) Manual sync。**v2.1.1 Known limitations(7 件)**: v2.1.0 リストから "Link migration tool is designed but not implemented" を削除し、代わりに "Per-candidate checkbox selection 未実装"、"Clickable-image renderer support 未実装(future dialect)" を追加、Card / embed / Color tag / Cross-container resolver / OS protocol handler / Full container footprint は継続。**新規 CHANGELOG_v2.1.1.md**(~170 行):Highlights / Normalize PKC links 詳細(導線 / 対象 3 種表 / 非対象リスト / 画面表示 / Apply 動作 / 6 種無効化条件 / Undo)/ Harbor-safe dialect gate(Candidate D 撤去理由 / clickable-image future 予約 / `[![]](target)` invalid 永続化)/ Manual sync 内訳(Slice 4 の作業をサマリ)/ Compatibility(schema 不変 / v2.1.0 ↔ v2.1.1 双方向互換 / 既存 body は user opt-in でのみ書き換わる)/ Known limitations(v2.1.1 版、v2.1.0 G4 External Permalink fragment scroll 未解消 / Per-candidate / clickable-image を future として明示)/ Migration 注意(アップグレード操作不要、Apply 後も revision restore で完全 rollback 可能)/ 参照 docs(link-migration-tool-v1 / clickable-image-renderer-audit / pkc-link-unification-v0 / v2.1.0 CHANGELOG / versioning-policy / INDEX #156-#161)/ 次 release 方針(v2.1.x patch = per-candidate / fragment scroll、v2.2.0 minor = clickable-image + migration v2 / card / embed / cross-container resolver 初期設計)。**quality gate**: typecheck / lint / test 5506 件全通過、build:bundle → bundle.js 684.17 KB(44.5% of 1536 KB budget)/ bundle.css 93.65 KB 不変、build:release → `dist/pkc2.html` 版数 `2.1.1-dev+<stamp>` を確認、build:manual → `PKC2-Extensions/pkc2-manual.html` も v2.1.1 を埋め込んだ sample container で rebuild、version 表示と About release block が v2.1.1 highlights / known limitations で更新されることを確認。src / scanner / dialog / reducer の実装変更は一切なし、Color tag / clickable-image / card / embed / SAFE_URL_RE / asset-resolver にも触れず。これで Link migration tool v1 は user-visible release として切り出され、About 画面 1 枚で "このバージョンで何ができて何ができないか" が即座に分かる状態に |

| 163 | `docs/development/import-export-surface-audit.md` (NEW) + `docs/development/INDEX.md` (MODIFIED) | Import / Export surface audit(docs-only、4 surface 再分類 + File Tree Interchange future 最小スコープ) | 2026-04-24 | v2.1.1(Link migration tool v1 完成)を切った後の、次 wave に踏み出す前の棚卸し docs-only slice。`src/adapter/platform/` と `src/features/import/` / `features/batch-import/` 系の全 export / import 経路を Explore agent で洗い出し、**22 surface(9 export + 11 import + 2 rehydrate)** を列挙。これらを harbor 視点の **4 つの Data surface**(入港 / 出港 / 退避 / 交換 = Import / Export / Backup ZIP / Interchange ZIP)に再分類し、各 surface に対応する既存導線 / 不足項目 / Internal / Legacy を明示。**重複・用語ブレ・UX 問題の棚卸し**(7 項目):Bundle / ZIP / Package の用語揺れ、`import-text-bundle` / `import-textlog-bundle` / `import-entry-package` の 3 導線重複、"export" が配布 / 復元 / 交換 / 移植の 4 用途で多重定義、Backup と Export の混在、Import の replace / merge / restore / batch / rehydrate / revision-restore の 6 経路分散、Light / readonly / editable 4 モードのユーザー可読性、folder-scoped / nested ZIP の中身同一性、Data panel top の 9-15 ボタン過剰。**推奨 user-facing surface**(案):Data メニューを 4 トップレベル(Import... / Export... / Backup ZIP... / File Tree ZIP...)に畳み、各モーダル内で advanced option(range / mode / format / target folder / structural relation restore)を提示、単体 bundle / archetype-scoped ZIP / context-menu folder export は top から外して詳細パネル quick action / Backup ZIP advanced に降ろす。**Default import = merge 原則**(§7、7 項目):現状の Replace default が破壊的であり harbor 原則(雲散霧消させない)に反する、preview 必須・Replace 切替時は明示確認、target folder 指定、structural relation 復元、flat fallback、conflict handling(既存 C1 / C2 / C2-multi を維持)、import provenance(既存 provenance relation 維持、body への source URL 埋め込みは見送り)。**Backup ZIP vs Interchange ZIP の分離**(§8):Backup ZIP = container.json + assets/ + revisions 全部含む完全復元用 `.pkc2.zip`(現行 `zip-export-contract.md` に準拠)/ Interchange ZIP = 人間可読 folder tree + `*.md` / `*.bin` + `_pkc_manifest.json` の `.pkc2-tree.zip`(未実装、v2.2+ future)、**同じ ZIP で兼用しない理由** を 3 点(human filename vs asset_key の正本性衝突、revisions の膨張、manifest の分岐)で明文化。**File Tree Interchange future**(§9、7 項目):v1 最小スコープ = Export のみ / TEXT / TEXTLOG(md + sidecar json)/ folder / attachment、relative link rewrite(asset: → ./assets/<file>、entry: → ./<relative>.md、tree 外は manifest に保持)、`_pkc_manifest.json` schema(schema_version / container / entries[relative_path / created_at / tags] / relations / assets、revisions は含めない)、SharePoint / OneDrive / Teams files 復元シナリオ(export → 外で編集 → merge import round-trip)、ZIP slip / zip bomb guard(path normalize / 圧縮率上限 / symlink reject / filename sanitize / manifest 不整合 warning)、実装は v2.2+ の別 wave で spec-first に着手する理由。**Proposed roadmap**(§10、非拘束 8 slice):α UI vocabulary cleanup / β Default import=merge / γ Backup ZIP 名称の正本化 / δ File Tree Interchange Protocol v1 spec / ε File Tree Interchange Export 実装 / ζ Import round-trip / η Export profile consolidation / θ Import preview unification。**non-goals**(§11):src / UI / reducer / spec の実装変更ゼロ、version bump ゼロ、既存 UI 文言書き換えゼロ、新用語の強制導入ゼロ、採否は個別 user レビューを経て確定。**References**(§12):manual 07 / 05 / 08、既存 spec(merge-import-conflict-*)、既存 development docs(zip-export-contract / text-markdown-zip-export / textlog-csv-zip-export / folder-scoped-* / container-wide-batch-import / batch-import-* / import-preview-ui / light-mode-badge-ui / selected-entry-* / merge-import-implementation / selective-import / provenance-metadata-copy-export-v1)、Link 系 audit(clickable-image-renderer-audit / link-system-audit-2026-04-24 / link-migration-tool-v1 / pkc-link-unification-v0)、CHANGELOG v2.1.0 / v2.1.1、src adapter/platform/ 8 ファイル、features/import/ / features/batch-import/、adapter/ui/export-handler.ts / zip-import-warnings.ts を列挙。**688 行 / 57 section**、src / tests / bundle / dist いずれも無変更、typecheck / lint / test / build は docs-only のため実行せず、本 audit は user レビューで採否を待って個別 PR に落とす方針 |

| 164 | `docs/spec/*.md` (MODIFIED, 28 本) + `docs/development/INDEX.md` (MODIFIED) | Repository Residual Cleanup Phase 1 — spec Status sync after v2.1.1 | 2026-04-24 | 2026-04-24 backlog audit(#163 前の棚卸し報告)の **R-01〜R-16 を中心とした docs-only Status 同期**。実装は src / UI / tests / bundle / dist / manual いずれも一切変更していない、version bump もなし。目的は「実装済み機能が `DRAFT` / `docs-only foundation` / `実装は別 PR` のまま残っていて次の AI / 人間が誤読する」状態の解消。**対象 28 spec**(全部 1 行 Status の書き換えのみ、本文は不変):`link-migration-tool-v1.md`(R-01、docs-only draft → v2.1.1 実装済み、Slice 1-4 + Phase 3 release bump 完了へ昇格)/ `pkc-link-unification-v0.md`(R-02、foundation draft → v2.1.1 Copy/Paste/Render/Receive 各層稼働中)/ `about-build-info-hidden-entry-v1-{minimum-scope,behavior-contract}.md`(R-03、2 本)/ `addressbar-paste-fallback-v1-{minimum-scope,behavior-contract}.md`(R-04、2 本、FI-08.x T-FBC-1..11 のテスト群が稼働)/ `addressbar-url-title-paste-v1-{minimum-scope,behavior-contract}.md`(R-04、2 本)/ `attach-while-editing-insert-internal-link-v1-{minimum-scope,behavior-contract}.md`(R-05、2 本、FI-05 attachment-presenter 稼働)/ `attachment-foundation-fi04-v1-{minimum-scope,behavior-contract}.md`(R-06、2 本、FI-04 attachment 基盤稼働)/ `dual-edit-safety-v1-behavior-contract.md`(R-07、dualEditConflict 系稼働)/ `entry-ordering-v1-{minimum-scope,behavior-contract}.md`(R-08、2 本、C-2 entry-ordering v1 稼働、HANDOVER_FINAL §22.1 landed 宣言確認)/ `image-intake-optimization-v1-{minimum-scope,behavior-contract}.md`(R-09、2 本、manual §画像の自動最適化)/ `link-index-v1-{minimum-scope,behavior-contract}.md`(R-10、2 本、C-3 link-index v1 稼働、HANDOVER §22.1 landed 宣言確認)/ `merge-conflict-ui-v1-behavior-contract.md`(R-11、conflict-detect / merge-planner 稼働、manual 07 §Merge mode)/ `revision-branch-restore-v1-{minimum-scope,behavior-contract}.md`(R-12、2 本、BRANCH_RESTORE_REVISION 稼働、manual 09 用語集)/ `search-entry-type-multi-select-v1-{minimum-scope,behavior-contract}.md`(R-13、2 本、archetype filter 稼働)/ `system-settings-hidden-entry-v1-{minimum-scope,behavior-contract}.md`(R-14、2 本、v2.1.0 で landing、system-settings-payload 稼働)/ `textlog-image-perf-v1-{minimum-scope,behavior-contract}.md`(R-16 部分、textlog staged render 稼働)。**Status 文言の統一パターン**:「**実装済み**(v2.1.0 以前に landing、v2.1.1 時点で稼働中)。本書は behavior contract / historical design record として保持。実装の現物は `src/...` / `tests/...` / ユーザー向け説明は `../manual/...`」の形に揃え、landing version が明確でないものは「v2.1.0 以前」と範囲表現、不明ならその旨を明記(捏造回避)。link-migration-tool-v1 / pkc-link-unification-v0 の 2 本は末尾の多行 Status ブロックを v2.1.1 の実態を反映する内容に書き換え(各 Future dialect reservations / CHANGELOG pointer を明示)。**見送った対象**(report で明示):(a) `folder-ctrl-s-repro-scope.md` は「Status: OPEN — 再現条件未確定」、再現未確認のバグスコープ doc のため OPEN 維持、(b) `textlog-replace-v1-behavior-contract.md` は既に `Status: ACCEPTED` で DRAFT ではないため触らず、(c) P1/P2 対象外だが同類で DRAFT が残っている `edit-safety-textlog-paste-target-and-folder-save-v1-minimum-scope.md` / `ui-theme-customizable-accent-scanline-v1-{minimum-scope,behavior-contract}.md` は次 round へ、(d) `color-tag-data-model-v1-minimum-scope.md` は spec 末尾に `docs-only` と明記、Slice 1 palette fixed list が未確定の状態で実装も意図的に未着手のため現状維持。**About と CHANGELOG の 2 件ずれ**(External Permalink body residue rendering / External Permalink receive fragment scroll が `build/about-entry-builder.ts` の 2.1.1 knownLimitations に欠落している一方 CHANGELOG v2.1.1 には記載あり)は **別 PR 推奨** として報告で明示、今回 PR 内では触らず(docs-only 契約維持 / dist + pkc2-manual rebuild を避けて review 単純化)。**quality gate**: grep で `Status: DRAFT` が P1/P2 対象外の 3 本にしか残らないことを確認、git diff で 28 spec が 1 行ずつのみ変更、本文は不変、docs-only のため typecheck / lint / test / build は不要(省略)。これで次の AI / 人間が spec を開いた際に "DRAFT のまま放置された実装済み機能" に騙されるリスクを消し、v2.1.1 時点のリポジトリ state が Status 行レベルで正確に読めるようになる |

| 165 | `build/about-entry-builder.ts` + `docs/development/INDEX.md` (MODIFIED) + `dist/bundle.js` + `dist/pkc2.html` + `PKC2-Extensions/pkc2-manual.html` (REBUILT) | RELEASE-CONSISTENCY FIX — About v2.1.1 Known limitations に 2 件追加(CHANGELOG 整合) | 2026-04-24 | #164 Repository Residual Cleanup Phase 1 で報告した `build/about-entry-builder.ts` の `RELEASE_SUMMARY['2.1.1'].knownLimitations`(7 件)と `docs/release/CHANGELOG_v2.1.1.md` §Known limitations(9 件)の 2 件ずれを解消する release-consistency fix。新機能実装ゼロ、version bump なし(`2.1.1` 維持)、schema_version 不変(`1` 維持)。**追加した 2 件**(CHANGELOG の文言を英語表記に寄せて About 形式で埋め込み):(1) `External Permalink body residue rendering is not implemented — a <base>#pkc?... string left inside body text is rendered as a plain external anchor; only boot-time receive resolves it`、(2) `External Permalink receive fragment scroll is not implemented — &fragment=log/<logId> on an External Permalink selects the entry but does not scroll to the log row`。**挿入位置**: OS protocol handler の直後、Full container footprint の直前(CHANGELOG §Known limitations の出現順と一致、関連性の高い項目を隣接させる)。CHANGELOG は source of truth として **本 PR では触らない**(既に正しい側)。`npm run build:bundle` で bundle.js 684.16 KB / bundle.css 93.65 KB(いずれも Phase 1 から不変)を確認、`npm run build:release` で `dist/pkc2.html` を version `2.1.1-dev+20260424104716`(schema 1)で再生成、`npm run build:manual` で `PKC2-Extensions/pkc2-manual.html`(1802.4 KB)を再生成し sample container の About にも 9 件の known limitations が埋め込まれることを確認。**quality gate**: `npm run typecheck` clean / `npm run lint` clean / `npm test` 5506 / 5506 pass(変化なし)、すべて問題なし。本 PR の diff は `build/about-entry-builder.ts` に 2 行追加 + `docs/development/INDEX.md` に 1 エントリ + `dist/bundle.js` / `dist/pkc2.html` / `PKC2-Extensions/pkc2-manual.html` の rebuild。**今回あえて触らなかった項目**: Repository Residual Cleanup Phase 2 の残り 3 spec DRAFT(`edit-safety-textlog-paste-target-and-folder-save-v1-minimum-scope.md` / `ui-theme-customizable-accent-scanline-v1-{minimum-scope,behavior-contract}.md`)/ src runtime の実装 / UI 文言 / manual 本文 / 新機能 wave / spec cleanup Phase 2。**これでユーザーが About 画面だけを見ても v2.1.1 の Known limitations を 9 件全て把握できる** 状態に揃った |

| 166 | `docs/spec/edit-safety-textlog-paste-target-and-folder-save-v1-minimum-scope.md` + `docs/spec/ui-theme-customizable-accent-scanline-v1-{minimum-scope, behavior-contract}.md` + `docs/development/INDEX.md` (MODIFIED) | Repository Residual Cleanup Phase 2 — 残り 3 spec DRAFT の Status 現実同期 | 2026-04-24 | #164 Phase 1(28 spec)+ #165 RELEASE-CONSISTENCY FIX の続きとして、**P1/P2 対象外だが DRAFT が残っていた 3 本**(#164 見送り対象で明示報告済み)を現実と同期する docs-only cleanup。実装確認を取った上で Status のみを最小書換、src / UI / tests / bundle / dist / manual / About / CHANGELOG / version いずれも不変。**調査対象 3 spec と判定**:(1) `edit-safety-textlog-paste-target-and-folder-save-v1-minimum-scope.md`(FI-02)— 1-A TEXTLOG 貼付先ズレは `src/adapter/ui/action-binder.ts` の `handlePaste` に `data-pkc-log-id` 付き selector を追加する形で **実装済み**(CLOSED 2026-04-18 commit `af43ab2`、audit `../development/textlog-paste-target-fix-audit.md` / `tests/adapter/action-binder-edit-safety.test.ts` 13 tests)、1-B FOLDER Ctrl+S は Chromium + happy-dom 両環境で **UNREPRODUCED** と確認され(`../development/folder-ctrl-s-browser-repro.md` repro matrix 全ケース SAVED)コード変更なしで close、両側とも **現実と乖離している DRAFT** のため Status を「実装済み / 再現不可クローズ」に書換。(2) `ui-theme-customizable-accent-scanline-v1-minimum-scope.md`(FI-12 v1)— `showScanline` / `TOGGLE_SCANLINE` / `SET_SCANLINE` / `accentColor` / `SET_ACCENT_COLOR` / `RESET_ACCENT_COLOR` の reducer paths、Shell menu の `renderShellMenu` 内 scanline + accent UI、`tests/adapter/fi12-scanline-kanban.test.ts`、audit `../development/ui-theme-customizable-accent-scanline-v1-audit.md`(v1 本体 commit `9078e90` CLOSED 2026-04-18)と `../development/fi12-followup-urgent-ux-audit.md`(follow-up commit `c68f1da`)で完全実装、**実装済み** に書換。(3) `ui-theme-customizable-accent-scanline-v1-behavior-contract.md`(同)— behavior contract も同じ実装経路で稼働中、同じ pattern で **実装済み** に書換。**Status 文言の統一パターン**:Phase 1 と同じ「実装済み(v2.1.x で稼働中)。本書は minimum-scope / behavior contract / historical design record として保持。実装の現物は `src/...` / `tests/...` / audit は `../development/...`」の形で、commit hash と audit doc pointer を明記して landing 根拠を読み戻せる状態に。**判定の安全性**:3 spec 全てについて grep + test 件数 + audit doc 本文を確認してから書換、user 指示の「確認なしに Status を書き換えないでください」を遵守、実装裏付けが取れなかった候補は **0 件**(今回は 3 本すべて実装確認可能だった)。**見送り判断は今回なし**(Phase 1 で既に見送りは分離済み、color-tag-data-model-v1-minimum-scope.md の末尾 docs-only Status は意図的に未着手の正本表明で触らず、その他 DRAFT 残存は Phase 1 + Phase 2 で全て処理完了)。**quality gate**: `grep -rn "^Status: DRAFT" docs/spec/` 結果は残存 **0 本**(意図的 docs-only の color-tag-data-model-v1-minimum-scope.md 末尾 Status のみ残るが、これは検出形式外で正当)、git diff は 3 spec + INDEX のみの 4 ファイル、本文無改変、docs-only のため typecheck / lint / test / build は不要。**これで Repository Residual Cleanup は Phase 1(28 本)+ Phase 2(3 本)+ RELEASE-CONSISTENCY FIX(About / CHANGELOG 整合)の 3 段階で完了**、v2.1.1 時点のリポジトリ state は Status 行レベルで完全に現実と同期した状態になる。次は PR #126 の状態確認(description / CI / changed files / review comments / mergeable state)に戻り、新機能 wave に進むかどうかは user + 統括役の判断待ち |

## Post-Stabilization Wave — 2026-04-19〜21

下の **Stabilization Phase — 2026-04-12** は当時の判断の履歴として残すが、
2026-04-19 以降の relations / references / provenance / orphan / P1–P5 /
hook subscription / transport wave が連続で landing したため、その節の
「新規実装よりもユーザからの新たな痛み待ちが妥当」という判断は現況を
反映していない。現況のサマリは:

- Relations / References / Backlinks 層: 9 docs shipped（#79–#87）
- Provenance metadata 層: 3 docs shipped（#88–#90）
- Unified Orphan Detection v3 / Connectedness 層: 5 docs shipped（#91–#95）、**S5 filter は Defer**（contract §7.4）
- Dead-code / dead-path maintenance: 8 docs shipped（#96–#103）
- P1–P5 wave: **P1 / P2 / P3 / P3 follow-up / P4 shipped**、**P5 は docs-only draft（implementation pending）**（#104–#110）
- Transport record: 2 docs shipped（#111–#112）
- Hook subscription: 4 docs shipped（#113–#116）、**結論は Defer**

次 wave の実装候補は `next-feature-prioritization-after-relations-wave.md`
および本 `CANDIDATE` 節 §P5 / §Hook subscription / §S5 を参照。

## Stabilization Phase — 2026-04-12

`project-priority-refresh.md` の棚卸し結果、直近のユーザ指摘は全て閉じ、残り候補は
いずれも「今やる妥当性が薄い」状態。**新規実装よりもユーザからの新たな痛み待ちが妥当**
と判定。以下の CANDIDATE 群は参照目的で保持する（優先度の昇格は新規報告後に行う）。

## CANDIDATE — Next Feature

### Relations / References / Provenance / Orphan / P1–P5 wave — 完了

本 CANDIDATE 節はかつて keyboard navigation の Phase 集約を主軸に書かれていたが、
2026-04-19〜21 の wave で軸は変わった。現況の候補は **P5 extension capture receiver side** が本命、
その他は **Defer**。詳細は `next-feature-prioritization-after-relations-wave.md`。

### Active candidate — P5 Extension Capture（receiver side）

| 項目 | 内容 |
|---|---|
| Status | **draft / pending implementation** |
| Draft | `extension-capture-v0-draft.md`（2026-04-21、docs-only、`record:offer` 再利用 Option B 推奨） |
| 次 PR 候補 | `docs/spec/record-offer-capture-profile.md` を docs-only で策定（payload spec 固定） |
| その後 | receiver 側実装（transport 拡張 / reducer capture action / provenance attach / origin allowlist / size cap / tests） |
| size | medium |
| arch risk | medium（transport 契約拡張 + 外部由来 sanitization） |

### Deferred — S5 Orphan Filter

| 項目 | 内容 |
|---|---|
| Status | **Defer**（canonical: `unified-orphan-detection-v3-contract.md §7.4`） |
| 根拠 | S4 sidebar marker で "気づき" は成立。filter は実需の積み上げ待ち |
| 昇格条件 | "orphan 一覧だけを取り出したい" 実需が明示されたとき |

### Deferred — Hook Subscription 実装

| 項目 | 内容 |
|---|---|
| Status | **Defer**（canonical: `pkc-message-hook-subscription-decision.md`） |
| 根拠 | review / PoC / acceptance / decision の 4 doc で論点は固定。実装前に simpler proof path（polling 等）を通す方針 |
| 昇格条件 | polling ベースの実用価値が検証され、かつ hook の追加投資に釣り合う pain が具体化したとき |

### Keyboard Navigation — Completion Snapshot

**Sidebar** (Phase 1–6): 完成

| Phase | Key | Action | Status |
|-------|-----|--------|--------|
| 1 | Arrow Up/Down | sidebar navigation | COMPLETED |
| 2 | Enter | begin edit | COMPLETED |
| 3 | Arrow Left/Right | collapse/expand | COMPLETED |
| 4 | Arrow Left (collapsed folder) | move to parent | COMPLETED |
| 5 | Arrow Right (expanded folder) | select first child | COMPLETED |
| 6 | Arrow Left (non-folder) | move to parent | COMPLETED |

**Kanban** (Phase 1 + 2 + 3): 完成

| Key | Action | Status |
|-----|--------|--------|
| Arrow Up/Down | 列内移動 | COMPLETED |
| Arrow Left/Right | 列間移動 (index clamp) | COMPLETED |
| Space | status toggle (open ↔ done) | COMPLETED |
| Ctrl+Arrow Left/Right | status move (directional) | COMPLETED |

**Calendar** (Phase 1): 完成

| Key | Action | Status |
|-----|--------|--------|
| Arrow Left/Right | 日移動 (空セルスキップ) | COMPLETED |
| Arrow Up/Down | 週移動 (±7 days, 空週スキップ) | COMPLETED |

**Summary**:
- Sidebar tree keyboard navigation は Phase 1–6 で完成
- Kanban keyboard Phase 1 (navigation) + Phase 2 (Space toggle) + Phase 3 (Ctrl+Arrow status move) 完了
- Calendar keyboard Phase 1 (navigation) 完了 — 日移動 + 週移動、月内限定
- 全 3 view で navigation 完成 + Kanban は action 操作も完成
- テスト合計 172 件（Sidebar 91 + Kanban 57 + Calendar 24）

### Keyboard Navigation — Not Implemented

- Calendar Phase 2 (month wrap, empty cell cursor)
- Shift+Arrow range selection

### Next Candidates

| | Calendar Phase 2 (month wrap) | Shift+Arrow range selection |
|---|---|---|
| ユーザ価値 | 低 — Phase 1 で主要操作は完了 | 中 — keyboard multi-select |
| コスト | 中 — re-render + 新 state 候補 | 高 — multiSelectedLids 統合 |
| リスク | 中 — scope 膨張 (wrap 範囲) | 高 — Phase 2-D 未解決 |
| 妥当性 | △ — 必要性が薄い | △ — 前提が未整備 |

設計: `calendar-kanban-keyboard-navigation.md` §9

### 保留候補

| 候補 | 保留理由 |
|------|---------|
| Phase 2-D: SELECT_RANGE 表示順対応 | Ctrl+click で代替可能。設計負債だが実害小 |
| Sidebar multi-DnD | structural relation の cycle detection 複雑化。BULK_MOVE で代替可能 |
| TEXTLOG drag-to-reorder | oldest-first storage 不変条件と衝突。設計変更議論が先 |
| Calendar Phase 2 (month wrap) | 必要性が薄い。Phase 1 で主要操作は完了 |
| Shift+Arrow range selection | Phase 2-D 未解決。前提が未整備 |

## Idea Inventory — 2026-04-12

`project-priority-refresh.md` 後に収集したアイデアを粒度ごとに分解・正規化。
実装は行わず、各 idea を 1 ファイル = 1 テーマで spec 化した。
カテゴリ間の参照は各 doc 末尾「将来拡張の余地」に記載。

### Category A — Immediate UX Improvements (`docs/development/`)

| # | File | Topic | Status |
|---|------|-------|--------|
| A-1 | `textlog-readability-hardening.md` | TEXTLOG 境界/日付/秒表示 | **COMPLETED 2026-04-12** |
| A-2 | `text-split-edit-in-entry-window.md` | entry window TEXT split edit | **COMPLETED 2026-04-14（S-13）** |
| A-3 | `table-of-contents-right-pane.md` | 右ペイン TOC | **COMPLETED 2026-04-12** |
| A-4 | `search-ux-partial-reach.md` | 検索 sub-location ヒット | **COMPLETED 2026-04-14（S-15 + S-18、FULLY PROMOTED）** |

**Stabilization re-entry (2026-04-12)**: A-1 + A-3 で「読める / 俯瞰できる」
まで到達。**A-2 / A-4 は 2026-04-14 に USER_REQUEST_LEDGER §1 で正式昇格・完了済み**。
Category A は全 4 件が完了に移行。

### Category B — Markdown / Rendering Extensions (`docs/development/markdown-extensions/`)

| # | File | Topic | Status |
|---|------|-------|--------|
| B-1 | `markdown-csv-table-extension.md` | CSV fenced block → table | **COMPLETED 2026-04-14（S-16）** |
| B-2 | `markdown-code-block-highlighting.md` | code block syntax highlight | **COMPLETED 2026-04-13（P-13 retrofit）** |
| B-3 | `markdown-quote-input-assist.md` | 引用入力補助 | **PARTIALLY COMPLETED — Slice α（continuation）2026-04-14（S-17）**。empty exit / bulk prefix / entry-window 同期は CONDITIONAL |

### Category C — Data Model Extensions (`docs/development/data-model/`)

| # | File | Topic | Status |
|---|------|-------|--------|
| C-1 | `revision-branch-restore.md` | revision 復元 | CANDIDATE（記録面は S-22 / H-6 で先置き済み） |
| C-2 | `entry-ordering-model.md` | entry 手動 ordering | **COMPLETED 2026-04-17（S-32）** |
| C-3 | `link-index-entry.md` | link index entry | **COMPLETED 2026-04-17（S-33）** |
| C-4 | `spreadsheet-entry-archetype.md` | spreadsheet archetype | CANDIDATE |
| C-5 | `complex-entry-archetype.md` | complex (composite) archetype | CANDIDATE |
| C-6 | `document-set-archetype.md` | document-set archetype | CANDIDATE |
| C-7 | `office-preview-strategy.md` | office file preview | CANDIDATE |

### Category D — Long-Term Vision (`docs/vision/`)

| # | File | Topic |
|---|------|-------|
| D-1 | `pkc-message-externalization.md` | entry message 送受信 |
| D-2 | `pkc-multi-window-architecture.md` | multi-window 協調 |
| D-3 | `webrtc-p2p-collaboration.md` | WebRTC P2P |
| D-4 | `pkc-application-scope-vision.md` | application scope 定義 |

## Close Audit Summary

- **First audit**: 2026-04-11 (broad scan)
- **Strict re-audit**: 2026-04-11 (per-item evidence with code/test/doc check)
- **CLOSE_NEEDS_REVIEW fixes**: 5 docs patched (container-wide-batch-import, folder-scoped-import, dnd-cleanup-robustness, textlog-polish, edit-preview-asset-resolution)
- **Initial result**: 42/42 CLOSED（2026-04-11 時点）
- **追補（2026-04-21）**: その後 #43–#116 が COMPLETED として順次追加され、本ファイル上で **42 CLOSED + 74 COMPLETED（relations / references / provenance / orphan / P1–P5 / hook / transport / dead-path maintenance を含む）** に拡大。close 再監査は次 PR で整理予定
